# Orphaned Games — Assessment of `namsbokasafn-leikir`

**Date:** 2026-08-15, substantially revised 2026-08-16
**Question:** the old repo `~/dev/repos/namsbokasafn-leikir` holds **33 game directories** (31 live + 2 archived); kvenno-app took **17** and wrote 3 more from scratch. What is in the **16** that were left behind, why were they left, and would any of them fit here?
**Companion:** `apps/games/1-ar/CURRICULUM_REVIEW.md` (the gaps these games are measured against)
**Method:** one agent per game, reading all source and data files, with file:line evidence required on every claim; every headline claim then re-checked by an adversarial verifier instructed to refute it. The causal section additionally used six forensic agents and three competing hypotheses argued independently before being judged.

> **Read the revision note.** The first version of this document (commit `583f073`) got the causal story wrong and shipped a chrome table that contradicted its own prose. Both are corrected below, and the corrections are itemised rather than quietly overwritten — see [Corrections to the first version](#corrections-to-the-first-version-of-this-document).

---

## Why they were left behind

**Nothing was judged and nothing was rejected. There was no selection event.**

The local clone of `namsbokasafn-leikir` had been sitting untouched since **2026-01-25 20:59** (`912d997`). On 2026-02-19 kvenno-app was built by copying what was on disk — and what was on disk was exactly the 17 games that came over. The 14 missing games were written by cloud/agent sessions (PRs #87–#100) in a four-day burst on **1–4 February**, straight to GitHub. They did not reach the machine until the pull on **2026-05-10**, 80 days after the migration.

That is why five independent searches found no commit message, no PR and no document explaining the choice. **No rationale was recorded because no decision was ever made.**

### The evidence

Established by measurement, and re-verified by hand:

- **Byte-level provenance.** For each of the 17 migrated games, `git ls-tree -r 914e6f5 -- apps/games/<g>/src` matches `git ls-tree -r 912d997 -- games/<g>/src` at **219/219 blobs, 100% per game**. Against the old repo's actual HEAD (`379266e`) the same trees hold 253 files with only 162 matches. Spot-re-verified independently: takmarkandi 17/17, hess-law 9/9, equilibrium-shifter 10/10. The migration source is fingerprinted to one specific commit.
- **The reflog freeze.** `.git/logs/HEAD` in the old repo: the last local move is `912d997` at epoch 1769374740 (2026-01-25 20:59:00); the next two entries are `reset: moving to HEAD` (1778414574) and `379266e … pull: Fast-forward` (1778414580), both 2026-05-10. **Zero activity spans 2026-02-19.**
- **The frozen tree is the migrated set.** `git ls-tree -d 912d997:games/{1,2,3}-ar` returns exactly 5 / 7 / 5 directories — precisely the 17 present at `914e6f5`, and precisely the "17 chemistry games (5 year-1, 7 year-2, 5 year-3)" that commit's own body claims.
- **Stripping post-dates migration.** No commit anywhere in the old repo — main, all 33 branches, both stashes — removes gamification chrome; the counts rise monotonically to its last commit. The stripping is two commits _here_: `bbbdd12` (2026-04-14 13:23, Y2/Y3) and `8ce63d5` (2026-04-14 16:16, Y1), two months after `914e6f5`. **The games arrived in kvenno-app with scoring and achievements fully intact.**

### The fact that settles it: the migration also amputated finished work from games that were kept

`git diff --name-status 912d997 379266e` over the 17 **kept** game directories shows **34 added `.ts`/`.tsx` files**, none of which ever entered kvenno-app on any ref (`git log --all --diff-filter=A` returns 0 commits for each of `Level4.tsx`, `Level5.tsx`, `ICETable.tsx`, `FactoryMode.tsx`, `MysteryMolecule.tsx`, `Pipette.tsx`, `EquationBuilder.tsx`, `QKChallenge.tsx`).

Among them: the `Level4.tsx` of lausnir, molmassi, takmarkandi, hess-law, kinetics, lewis-structures, intermolecular-forces, organic-nomenclature and ph-titration (counted as eight in one pass and nine in another; the discrepancy is not resolved); molmassi's `Level5.tsx` + `MysteryMolecule.tsx` + `data/conversionChains.ts`; takmarkandi's `FactoryMode.tsx` + `StoichiometryVisualization.tsx`; equilibrium-shifter's `ICETable.tsx` + `QKChallenge.tsx` + `utils/ice-table.ts`; lausnir's `Pipette.tsx` + `IndicatorSystem.tsx` + `data/saturation.ts`; hess-law's `EquationBuilder.tsx`; dimensional-analysis's `Level4Chemistry.tsx` + `UnitConversionBuilder.tsx`.

Verified absent today: no `Level4` or `Level5` exists under `apps/games/{1-ar/molmassi,1-ar/takmarkandi,3-ar/ph-titration,3-ar/equilibrium-shifter}/src/components/`, and a grep for `ICETable|ICE tafla|jafnvægistafla` across all 20 shipped games returns zero.

**No curator selecting for completeness removes Level 4 from a game they are carrying over. A stale snapshot does it automatically.** This work is recoverable from the old repo at `379266e`.

**Two rows of `CURRICULUM_REVIEW.md` are wrong-as-diagnosed because of it:**

- `CURRICULUM_REVIEW.md:72` — "Percent yield ○ **Advertised but never taught** — `takmarkandi/src/i18n.ts:11` promises 'reikna heimtir'; nothing in the game delivers it." The old `games/1-ar/takmarkandi/src/components/Level4.tsx` imports `PERCENT_YIELD_PROBLEMS`, `calculateTheoreticalYield` and `calculateActualYield` from `data/molarMasses.ts`, which holds 8 problems with `actualYieldPercent` values. **The promise was true when written.** That i18n line is an amputation scar, not false advertising.
- `CURRICULUM_REVIEW.md:68` — "No gram→mole→ratio→mole→gram calculation exists anywhere in Y1." The old `games/1-ar/molmassi/src/data/conversionChains.ts` opens `// Mole Conversion Chain Problems / Mass → Moles → Molecules → Atoms` and drives `Level5.tsx`; `Level4.tsx` + `avogadro.ts` cover Avogadro's number with scientific-notation input parsing.

Keep the distinction sharp: `C3-yield` and the mole-chain row are **taught, then lost**. `C4-electrolyte`, `C3-empirical`, `C4-precip`, net-ionic, isotopes and separation techniques are **genuinely empty** — re-tested on Icelandic stems (`rafkleyf`, `rafleiðar`, `raflausn`, `jónast`, `reynsluformúl`, `botnfall`, `útfelling`, `leysnireglur`, `samsæt`, `massatal`, `eiming`, `litskilj`, `síun`, `aðskilnað`) across all 20 shipped games' `src/` and their built HTML, with zero or false-positive-only hits. `(aq)` returns 12 hits, **all Year 3** — so the Y1 claim holds, and Y3 uses a notation Y1 never introduces.

### What is inference, and what is not determined

**Inference:** that the stale checkout is the _whole_ mechanism. It explains 31 of 33 directories. `games/archive/ph-titration-master` and `ph-titration-practice` were first committed 2025-11-28/29 (`9d59d66`, `d37ec64`), **were** on disk at `912d997`, and still did not migrate. A second filter ran — a path glob over `games/{1,2,3}-ar/*`, or the frozen 17-entry `scripts/build-games.mjs` (last touched 2025-12-31, `3c47726`, listing exactly those 17) — and which one is **not determined**; `914e6f5` is a squashed commit preserving no import script.

**Not determined:** whether the February PRs were known to exist on 2026-02-19. If they were reviewed and passed over, a deliberate-selection reading reopens; the reflog implies they were out of sight. Also not determined: what "completed" meant subjectively. Every contemporaneous document in the old repo marks all 27 games complete and production-ready (`repository-status.md:185`, `:235`; `docs/_generated/games.md:49`), so an unwritten personal standard can be neither confirmed nor refuted — though it remains true that no such standard was _applied_, because the 14 games were not present to filter.

### Chrome did not predict migration

The first version of this document asserted that "the migrated games are the chrome-heaviest in the old repo" and printed a table scoring every orphan at zero on timer, streak, confetti, sound and penalty. **Both halves are wrong**, and they contradicted this document's own prose forty lines further down.

Re-measured across all 33 projects: the direction holds on average (migrated mean 3.35 vs orphan 1.50 on those five patterns) but there is no separation. **Seven of the 17 migrated games score exactly zero**, and the orphan `flokkun-efna` scores 5, outscoring 12 of the 17 migrated games. The omitted non-zero orphans: `flokkun-efna` timer=2/streak=3, `gerdir-efnahvarfa` timer=2/streak=3, `jonir-i-lausn` streak=3, `markverdir-tolustafir` timer=1, `sydur-og-basar` streak=4. Sorted honestly, the boundary vanishes.

The zeros were a measurement artefact. The regex `penalty|deduct` structurally cannot match a labelled Icelandic cost — `uppbygging-atomanna` scored penalty=0 while shipping `Sýna vísbendingu (-50 stig)` at `Level1.tsx:365`, `Level3.tsx:260` and `i18n.ts:100` — nor the reduced-award form (`hlutfallsgreining/src/components/Level3.tsx:84-85`, `showSteps ? 5 : showHint ? 7 : 10`). Twelve files across seven orphans carry a `(-N stig)` label. **The prose was correct; the table was measuring an English-identifier vocabulary that could not see what the prose described.** It has been removed rather than patched.

None of the 16 left-behind games ever entered kvenno-app: `git log --all` returns zero commits touching any of their paths. They were never dropped or rejected — they simply were not in the tree that was copied.

None of the orphans ever entered kvenno-app: `git log --all` returns zero commits touching any of their paths. They were dropped at consolidation, not deleted later, and nothing in either repo records the decision.

### Then three games were written fresh, here

Consolidation brought **17** games, not 20. The other three were **created in kvenno-app on 2026-04-14**, in the very same two commits that stripped the chrome:

| Commit    | What it did                                                                                                                                                    |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `8ce63d5` | "strip gamification chrome from y1 games, **add jafna-jofnur and lotukerfid games**" — chrome removed from _all 5_ Y1 games, and two new games added           |
| `bbbdd12` | "strip gamification chrome from y2/y3 games, **add rafeindabygging game**" — chrome removed from all 12 Y2/Y3 games, plus a new Ch-6 electronic-structure game |

Two of those three duplicate topics that already existed in the old repo, and both were **written fresh rather than ported** — different data models entirely:

- `jafna-jofnur` (new) vs `stilltu-efnajofnur` (old). The new `data/reactions.ts` stores per-molecule element-composition maps so atoms can be counted programmatically — which is what makes the live `AtomCounter` possible. The old `data/equations.ts` opens with an `ELEMENT_COLORS` table and a `ReactionType` union. The rewrite is the better design.
- `lotukerfid` (new, 1836 LOC) vs `lotukerfid` (old, 1693 LOC). Independent implementations; the new one adds a reusable `PeriodicTable.tsx`.

**One regression came out of that rewrite** — and it is the cheapest recoverable item in this entire document.

The old `lotukerfid` shipped `src/data/trends.ts`: exactly 200 lines, verified by hand. It defines three trend types with an Icelandic and English name, description and **rule** each — `Atómgeisli` ("minnkar eftir lotu og stækkar niður hópa"), `Jónunarorka` ("eykst eftir lotu og minnkar niður hópa"), `Rafneikvæðni` ("eykst eftir lotu og minnkar niður hópa (eðalgös undanskilin)") — plus **12 paired comparison questions**, four per trend, each with an Icelandic explanation. All three rules are correct as stated, and **all 12 answer keys verify correct**: Na>Cl, Na>Li, S>O, C>F on radius; F>Li, Na>K, Mg>Ca, N>B on ionization energy; Cl>Na, F>Cl, O>C, Br>K on electronegativity.

It also uses `Rafneikvæðni` — the term this codebase already uses elsewhere — not the `rafdrægni` that returns zero hits.

The rewrite dropped it and nothing replaced it. `grep -rn "Atómgeisli|Jónunarorka|Rafneikvæðni|atomic-radius|ionization" apps/games/1-ar/lotukerfid/src/` returns **nothing**.

**Verdict on old `lotukerfid`: harvest-content-only, effort S.** Lift `trends.ts` into `apps/games/1-ar/lotukerfid/src/data/` and give it a level. Nothing else in the old implementation is worth taking — the shipped rewrite is the better architecture, as noted above.

**One correction to a natural misreading:** the old `lotukerfid` does **not** fix the shipped game's neutron blocker (`CURRICULUM_REVIEW.md` B1). It contains no neutron counting at all — `grep -rn "nifteind|neutron|massatala|massNumber|samsæt|isotope"` over its `src/` returns zero hits, and the shipped bug is still live at `apps/games/1-ar/lotukerfid/src/components/Level3.tsx:31-32`, `Math.round(el.atomicMass) - el.atomicNumber`. The isotope content that fixes B1 is in `uppbygging-atomanna`, a different game.

The grep behind that claim was partly wrong and has been re-run. `rafdrægni` returns **0** hits — it is not the term this codebase uses; `rafneikvæðni` returns 2. The original conclusion survived only because `electronegativ` was in the same alternation. And "only an unused i18n label in IMF" is wrong: `apps/games/2-ar/intermolecular-forces/src/components/Level1.tsx:24` puts `'Rafneikvæðni'` in `RELATED_CONCEPTS.dipole`, spread into rendered output at `:911` when `molecule.isPolar` — live UI, not a dead label. **The core claim survives: periodic trends are nowhere _taught_ in the platform.**

**Consequence:** all 16 left-behind projects are frozen at the **pre-restructure state**. Every defect the April 2026 restructure removed from the shipped games is still present in these, for the simple reason that the restructure happened here and they were not here to receive it.

---

## The shared defect set — and why "fix the template" is not available

Every one of the seven Year-1 orphans shares the same defect set, and it is the same set the curriculum review found in the shipped games. The first version of this document attributed that to a shared code template. **That premise is false, and it was checked:** `tools/game-template/src/App.tsx` is 175 lines, imports only `useProgress`, `useAccessibility`, `useGameI18n` and `LanguageSwitcher`, and has no score, no streak, no timer, no hint cost, no level gate, no achievements and **no question of any kind** — its game screen is a placeholder div. Of the eight items below, only #6 (by omission) and #8 are template-inherited.

The defects really are shared across the orphans; the common ancestor is a **generation prompt, not a file**. The practical consequence is that there is no single upstream fix — each game carries its own copy of the pathology and must be treated individually.

1. **The answer is on screen before the student answers** — in _all seven_. `flokkun-efna`: only pure substances carry a `formula` field, and it renders above the buttons, so the formula alone resolves half the four-way choice. `jonir-i-lausn`: all 15 Level-1 descriptions name the category in words ("Sterk sýra sem leysist algjörlega"). `sydur-og-basar`: Icelandic acid/base names are suffix-transparent and the name prints above the two buttons. Combined with the four leaks already found in the shipped games, **11 of 12 Y1 games leak the answer**.
2. **Score, streak counters and streak bonuses** through every level.
3. **Hints that cost points**, several advertising the penalty in the button label (`'Sýna vísbendingu (-50 stig)'`).
4. **Completion- or score-gated levels** with padlocks — ironically the one thing the shipped games _should_ have kept.
5. **Three levels that are the same task with progressively less support** — the opposite of a scaffolding fade.
6. **No shared site chrome** — no Header, Breadcrumbs, Footer, FeedbackPanel or HintSystem; each ships its own gradient shell.
7. **Achievements chrome** wired in throughout. The exports do still exist in `packages/shared/`, so this is dead weight rather than a broken import.
8. **Bilingual/trilingual UI** with a rendered LanguageSwitcher, against the Icelandic-only rule.

**Diacritic-stripped ASCII Icelandic** (þ→th, æ→ae, ð→d) ships as rendered UI in `uppbygging-atomanna` (all three level components) and `sydur-og-basar`. Two corrections to the first version: the counts appear to have been swapped — a consistent token detector gives ~20 for `sydur-og-basar` and ~45 for `uppbygging-atomanna` — and **treat both figures as unverified in either direction**, since any token list undercounts and `sydur-og-basar` mixes correctly-rendering HTML entities with genuinely stripped ASCII, sometimes on one line (`Level3.tsx:286`, `Retta svari&eth;`: the entity renders, `Retta` does not). More importantly, this is **not an orphan trait**: of the seven old-repo projects that ship it, five are migrated or superseded (`lausnir`, `molmassi`, `vsepr-geometry`, `equilibrium-shifter`, old `lotukerfid`) and only two are orphans. `apps/games/2-ar/vsepr-geometry` was still shipping it at the time of measurement.

---

## Corrections to the first version of this document

Every claim below has been fixed in place above. It is itemised here because a document that quietly rewrites itself teaches you nothing about how much to trust its next version.

| What the first version said                               | What is true                                                                                                                                                                                                                                                                                                          |
| --------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| "32 game projects; kvenno-app kept 20"                    | 33 directories (31 live + 2 archived). **17** migrated; the other 3 kvenno games were written fresh on 2026-04-14.                                                                                                                                                                                                    |
| "the 12" orphans                                          | **14** left behind (the 12 named, plus `stilltu-efnajofnur` and old `lotukerfid`); **16** counting the archive pair. The first version discussed two of them without counting them.                                                                                                                                   |
| "The migrated games are the chrome-heaviest"              | Direction holds on average, but 7 of 17 migrated games score zero and the orphan `flokkun-efna` outscores 12 of them. Chrome has no discriminating power.                                                                                                                                                             |
| The chrome table                                          | Cherry-picked: the "orphans" row aggregated 7 of 12 and omitted the five non-zero ones; the migrated rows were the four heaviest of 17. Removed.                                                                                                                                                                      |
| Table says penalty=0, prose quotes `(-50 stig)`           | **The table was the defective half.** Its regex could not match a labelled Icelandic cost. The prose was right.                                                                                                                                                                                                       |
| "exactly 5 source files each"                             | False of the source trees: hlutfallsgreining 10, uppbygging-atomanna 10, calorimetry 8, electrochemistry 8, organic-reactions 8, solubility-equilibrium 12, ka-kb-jafnvaegi 14.                                                                                                                                       |
| "Orphans: 1788–2895 LOC … 2–15 files"                     | Orphans measure 1902–3009 LOC; migrated `src/` file counts are 8–22.                                                                                                                                                                                                                                                  |
| "no exceptions in either direction… the turn of the year" | Refuted twice over. The archive pH pair is from November 2025 and did not migrate. The real cut is **2026-01-25 20:59**, not the year line — they coincide only because no game was created between 2025-12-29 and 2026-02-01, which makes the year-line claim unfalsifiable exactly where it differs from the truth. |
| "What predicts migration is when the game was written"    | Correlation right, mechanism wrong. Date is collinear. The operative variable is **presence in the local working tree on consolidation day**.                                                                                                                                                                         |
| "dropped at consolidation… nothing records the decision"  | Nothing was dropped or rejected. The second clause is right for the wrong reason: no rationale exists because no decision was made.                                                                                                                                                                                   |
| "inherited from the template"                             | The template is 175 lines with no scoring, no gating and no questions. The shared ancestor is a generation prompt, not a file — so there is no upstream fix.                                                                                                                                                          |
| "three wrong answer keys in Level 2"                      | Only `l2-5` is a key edit; `l2-1` needs corrected percentages; `l2-10` must be regenerated.                                                                                                                                                                                                                           |
| "reaction-type names contradict the school's textbooks"   | Not verifiable — no textbook is named. Half the fix is mechanical from the glossary; half needs a decision.                                                                                                                                                                                                           |
| "`sydur-og-basar` across ~45 strings"                     | Figures appear swapped (~20 vs ~45), and both are unverified. Also not an orphan trait — 5 of the 7 projects shipping ASCII Icelandic are migrated or superseded.                                                                                                                                                     |
| the periodic-trends grep                                  | `rafdrægni` returns 0 hits; the codebase uses `rafneikvæðni`. The IMF hit is live UI, not a dead label. The conclusion survives.                                                                                                                                                                                      |
| "The exports do still exist in `packages/shared`"         | **Correct, and re-confirmed.** The achievements surface is fully intact and still exported, with zero production importers under `apps/`.                                                                                                                                                                             |
| — _omission_                                              | The consolidation also lost **34 finished files from games that did migrate**. It is the fact that refutes every judgment-based explanation, and the first version never mentioned it.                                                                                                                                |

---

## Year 1 — the seven orphans

Ordered by value to the curriculum. This section was written on a **curriculum-and-correctness** axis; the four-axis assessment (structure/pedagogy, UX/chrome, coverage, fit) for these same seven is in [The leftover games, assessed](#the-leftover-games-assessed) below.

### 1. `jonir-i-lausn` — the highest-value orphan → ~~migrate (M)~~ **revised: rebuild from content (L)**

Teaches electrolyte classification (strong/weak/non), solubility rules, and precipitation — Brown 4.1–4.2. Hits **two of the four gaps that no game in any year fills**: `C4-electrolyte` and `C4-precip`, both _deep_.

- Level 1's teach phase is the best in the batch: three clickable conductivity-tester SVGs expanding to definitions plus worked dissociation equations.
- 49 data records across three files.
- **Does not** teach net ionic equations, despite `package.json:4` claiming "nettó jónajöfnur" — net ionic equations are _printed_ after the student has answered, and "áhorfandajón" (spectator ion) appears nowhere. So the largest single gap stays open unless that level is written.
- Blockers: the Level-1 description leak (must be rewritten, not tweaked), a Level-2 hint that _is_ the answer sold for 50 points, and Level 3 testing precipitation with no teach phase.

### 2. `hlutfallsgreining` — ~~migrate (M)~~ **revised: rebuild from content (L)**

Percent composition → empirical formula → molecular formula. Fills `C3-fw` and `C3-empirical`, the latter confirmed absent from all three years. 30 fixed items.

- Response format is free construction throughout — no multiple choice anywhere.
- **Three defective Level-2 items**, but only one is a key edit — the first version of this document overstated the fix. `l2-5` (`data/compounds.ts:303`) is a genuine key error: N 35.00 / H 5.04 / O 59.96 gives N₂H₄O₃, the key says `NH₂O₃`. `l2-1` (`:262`) is named "Vetnis peroxíð grunnformúla" and feeds `l3-2` (`:394`), so the **percentages** are the defect, not the key (should be H 5.93 / O 94.07). `l2-10` (`:354`) yields no clean formula at all — Mg 28.83 / P 22.04 / O 49.13 against real Mg₃(PO₄)₂ at 27.74 / 23.57 / 48.69 — **no key edit fixes it; the data must be regenerated.** The `l2-2`/`l2-8` duplicate is confirmed; `l2-3,4,6,7,9` and all of L3 recompute correct.
- Entirely test-first: all three levels open on problem 1 with an empty box; the only exposition is a four-bullet objectives list on the menu.
- L1 has no skip, so a student stuck on Al₂(SO₄)₃ can never unlock L2.

### 3. `uppbygging-atomanna` — **rebuild from content** (M)

Subatomic particles, **isotopes**, and average atomic mass as a weighted average — Brown 2.3–2.4. Fills `C2-isotopes`, and its Level 3 teaches exactly the mass-number-vs-weighted-average distinction that **blocker B1 in Lotukerfið currently gets wrong**. Fixing B1 and filling this gap are the same piece of work.

- Free numeric construction throughout, never multiple choice; teach-before-test at every level.
- Ions deliberately excluded (neutral atoms hardcoded), so it does not also fill `C2-ions`.
- Blockers: ASCII-stripped Icelandic in all three levels; L1 and L2 are mechanically the same exercise; the Bohr model is a static SVG in a level titled "Byggðu atómið" where the student never places a particle.

### 4. `markverdir-tolustafir` — ~~rebuild from content (M)~~ **revised: harvest content only (M)**

Significant-figure counting rules and the +/− vs ×/÷ rules, plus a third level on scientific notation — which no game in the monorepo covers at all. Fills `C1-uncert`, currently _graded but never taught_ in Einingagreining.

- All 44 items verified sound.
- Teaches sig figs as **digit-counting, not as a property of a measurement**: every item is a bare number with no units, no instrument, no measurement context. Precision vs accuracy is never taught.
- Grading is `parseFloat` equality, which is semantically wrong for a sig-figs game — `1.50` and `1.5` compare equal. Needs rewriting regardless.
- Blockers: score-gated levels (≥500 points), a 30-second countdown in Level 3, trilingual i18n.

### 5. `sydur-og-basar` — **rebuild from content** (M)

Arrhenius acid/base classification, neutralization with balanced equations, and neutralization solution stoichiometry. Fills `C4-acidbase` (neutralization as a reaction class) and the **stoichiometric half of `C4-soln-stoich`** — both named gaps.

- All 10 neutralization equations balance; all 8 stoichiometry answers recomputed correct, including the 1:2 and 2:1 ratio cases. ~33 verified items.
- Structurally already teach-before-test.
- No pH, no indicators, no titration, no dissociation, and **not one `(aq)` symbol**.
- Blockers: Level 1 invalid as assessment (answer printed above the question, all 15 items); Level 3's grader accepts ±0.5 absolute regardless of unit, making both 0.10 M problems unfailable; ~45 ASCII-Icelandic strings.

### 6. `flokkun-efna` — **rebuild from content** (M)

Classification of matter: element / compound / homogeneous / heterogeneous, 24 items evenly split 6/6/6/6. Fills `C1-matter`, currently used as an unexplained prerequisite. All 24 classifications are defensible, and the O₂ hint handles the diatomic-element trap well.

- **The central definition is wrong.** Homogeneous vs heterogeneous is defined by _visibility_ (`classifications.ts:50,58`) rather than by composition varying point to point. Under that rule any uniform-looking colloid — milk, mayonnaise, fog — classifies as homogeneous. Must be rewritten before use.
- Rust given as Fe₂O₃ (real rust is hydrated, Fe₂O₃·nH₂O, and in practice a mixture) — and it is a visible counterexample to the game's own rule sitting in the pure-substance bin.
- "Málmblöndun" is the process, not the material: should be **málmblendi**. Three student-facing occurrences.
- States of matter never appear; `C1-props` (physical vs chemical change, separation techniques) is absent.
- Structure is one MCQ task repeated three times with less support each time, and both answer leaks are properties of that shape — they cannot be patched out.

### 7. `gerdir-efnahvarfa` — ~~rebuild from content~~ **revised: harvest content only (M)**

Five reaction patterns — synthesis, decomposition, single replacement, double replacement, combustion — over 20 pre-balanced equations. Fills `C3-rxntypes`, and sits exactly on the Y1 chain between Jafna Jöfnur and Takmarkandi.

- All 20 equations verified balanced, including 2C₈H₁₈ + 25O₂ → 16CO₂ + 18H₂O.
- **The Icelandic reaction-type names need a terminology pass**, and they are referenced by every level, the summary and all 20 hints. The first version claimed they "contradict the school's textbooks" — that is not verifiable from either repo, since no textbook is named. What _is_ checkable: `Sundurliturarhvarf` (`i18n.ts:46`, `:69`, `data/reactions.ts:48`) is not an Icelandic chemistry word, the glossary supplies `decomposition reaction;niðurbrotsefnahvarf` at `packages/shared/i18n/ordabok.md:132`, and **the game already uses that root** in three item names (`reactions.ts:176,191,206`). So about half the terminology blocker is mechanically fixable today. The genuinely blocked half: the glossary has no entry for synthesis, replacement, displacement or metathesis — that needs your decision.
- All three levels are the same 5-way MCQ; Level 1's genuinely good explore phase is optional and unassessed, and the quiz button is visible from first render.
- Level 3 is a mandatory 12-second-per-question countdown behind a completion gate.

---

## Recommended sequence (Year 1) — superseded

_The sequence originally printed here was written before the four-axis assessment below, which revised four of its seven verdicts and re-ordered the work. Use [Recommended order of work](#recommended-order-of-work) at the end of this document instead._

---

## The leftover games, assessed

Fifteen game directories in `namsbokasafn-leikir` were never seen here — thirteen written by cloud sessions in the 1–4 February burst that reached the machine 80 days after consolidation, plus the two archived pH variants. **Nothing below is a quality judgment that was made and is being revisited. No one ever looked.** All fifteen were built against today's toolchain by a forensic pass: all fifteen compile and bundle clean against `packages/shared`, React 19.2.8, Vite 8.2.1 and Tailwind 4.3.3 with zero TypeScript errors; no shared export any of them imports has been deleted; the build-config port is a 28-line `vite.config.ts` collapsing to a 6-line `createGameViteConfig` call. **The mechanical cost of bringing any of these here is known and low.** Every verdict below is therefore about pedagogy, content and curriculum — what would have to be _rewritten_ for the game to belong, not what would have to be _rewired_ to compile.

### Summary

| Game                     | Year    | Topic                                                        | Covered here today?                                                        | Verdict              | Effort                            |
| ------------------------ | ------- | ------------------------------------------------------------ | -------------------------------------------------------------------------- | -------------------- | --------------------------------- |
| `calorimetry`            | Y2      | q=mcΔT, coffee-cup and bomb calorimetry                      | **absent** — 30+ terms, zero hits                                          | rebuild-from-content | L                                 |
| `uppbygging-atomanna`    | Y1      | Subatomic particles, isotopes, weighted atomic mass          | isotopes/weighted average **absent**; p/n/e **touched, taught wrongly**    | rebuild-from-content | M narrow, L with an Explore phase |
| `jonir-i-lausn`          | Y1      | Electrolytes, solubility rules, precipitation, net ionic     | **absent** — all four, 34+ greps                                           | rebuild-from-content | L                                 |
| `hlutfallsgreining`      | Y1      | Percent composition → empirical → molecular formula          | empirical **absent** all three years; % composition **touched**            | rebuild-from-content | L                                 |
| `markverdir-tolustafir`  | Y1      | Significant figures, sig-fig arithmetic, scientific notation | counting **touched, never taught**; arithmetic and notation **absent**     | harvest-content-only | M                                 |
| `solubility-equilibrium` | Y3      | Ksp, molar solubility, common ion, Q vs Ksp                  | **absent** — `Ksp` has zero real occurrences platform-wide                 | rebuild-from-content | L                                 |
| `ka-kb-jafnvaegi`        | Y3      | Ka/Kb expressions, weak-acid pH, percent ionization          | **absent**; pKa taught 425× as a given, never derived                      | rebuild-from-content | L                                 |
| `electrochemistry`       | Y2      | Galvanic cells, E°, Faraday, electrolysis                    | cells **touched**; Faraday/Nernst/electrolysis/activity series **absent**  | rebuild-from-content | L                                 |
| `sydur-og-basar`         | Y1      | Acid/base classification, neutralization, soln stoichiometry | classification **absent**; neutralization **touched**; L3 **taught** in Y3 | rebuild-from-content | M                                 |
| `organic-reactions`      | Y2      | Addition/substitution/elimination, reagent selection         | **absent** — organic reactivity taught nowhere                             | harvest-content-only | M                                 |
| `flokkun-efna`           | Y1      | Element/compound/homogeneous/heterogeneous                   | **absent** (14 terms); element/compound used as undefined prerequisite     | rebuild-from-content | M–L                               |
| `gerdir-efnahvarfa`      | Y1      | Five reaction types                                          | classification as a skill **absent**; 8 of its 20 equations already ship   | harvest-content-only | M (the harvest itself is S)       |
| `stilltu-efnajofnur`     | Y1      | Balancing equations                                          | **taught** — superseded by `jafna-jofnur`                                  | harvest-content-only | S                                 |
| `ph-titration-master`    | Y3-arch | Titration curves, indicators                                 | **taught** — it _is_ the shipped game's ancestor                           | skip                 | S                                 |
| `ph-titration-practice`  | Y3-arch | Titration sandbox                                            | **taught**                                                                 | skip                 | S                                 |

**The real headline is not a game. It is that reading these fifteen surfaced thirteen live defects in the shipped platform**, several of which teach students wrong chemistry today: `lotukerfid` Level 3 computes neutrons as `Math.round(atomicMass) − Z` (`apps/games/1-ar/lotukerfid/src/components/Level3.tsx:31-33`) and teaches that rounding as _the method_, mis-grading a neutron question in ~31% of playthroughs and showing a wrong neutron count in ~54–60%; `ph-titration` divides molarity by 1000 in every initial-pH branch (`src/utils/ph-calculations.ts:57,95,133,198`), rendering pH 4.37 for 0.100 M acetic acid where its own data file asserts 2.87; and 4 of its 6 Level-2 puzzles load a titration that contradicts their own task text, one of which marks the correct indicator wrong. Those are afternoon fixes and they are worth more than any port.

Of the fifteen games themselves, the genuine gains are **calorimetry** (the only orphan topic corroborated by shipped production content — a live AI-graded Kvennó lab, `apps/lab-reports/src/config/experiments/orka_2ar.ts`, grades students on material no game teaches), **isotopes and weighted atomic mass** (which fixes the `lotukerfid` bug as the same piece of work), and the **absent quantitative equilibrium block** (Ksp, Ka/Kb) that Y3 currently assumes without ever deriving. What is genuinely not worth taking: the two pH variants (byte-identical ancestors of what ships), `stilltu-efnajofnur` (superseded, ~5 novel equations), `organic-reactions`' entire Level 2 (SN2/E2 arrow-pushing, no Brown home, half of it leaks from its own reference panel), and — in every one of the fifteen — the code.

**Four verdicts in this document's Year-1 section above are revised by this deeper pass.** `jonir-i-lausn` and `hlutfallsgreining` were rated **migrate**; both are **rebuild-from-content**, because in each case the defect that disqualifies them is structural rather than patchable (a data-schema split, and a scaffold that _is_ the answer leak). `markverdir-tolustafir` and `gerdir-efnahvarfa` were rated **rebuild-from-content**; both are **harvest-content-only**, because after the cuts there is no shell left to rebuild — only a data file. Effort for `jonir-i-lausn` and `hlutfallsgreining` rises from M to L for the same reason.

---

### Year 1 (eight games)

#### 1. `uppbygging-atomanna` → **rebuild from content** (M narrow, L with an Explore phase)

**The deciding fact: its Level 3 teaches exactly the distinction the shipped `lotukerfid` currently gets wrong.** `apps/games/1-ar/lotukerfid/src/components/Level3.tsx:31-33` is `Math.round(el.atomicMass) - el.atomicNumber`, and it is not an incidental helper — the hint at `:106` ("Rúnaðu frumeindamassann og dragðu frá atómnúmerinu") and the intro at `:262` teach it as the method. Of the 36 elements in its pool, 7 mismatch; 6 of those round to radioactive nuclides (Ni-59, Cu-64, Zn-65, Ga-70, Se-79, Br-80), so a student who correctly answers 34 neutrons for copper is told 35, with the explanation printing `64 − 29 = 35`. Drawing 2 neutron slots from 36, ~31% of playthroughs mis-grade; across all 4 neutron-derived slots, ~54–60% display a wrong neutron number. The orphan's `Level3.tsx:157-163` is a ready-made correction: "Massatala er EKKI það sama og atómmassi á lotukerfinu … Þess vegna er atómmassi klórs 35.45 u, ekki 35 eða 37."

**Structure.** Free numeric entry throughout, never MCQ. Teach-before-test at all three levels (`'learn' | 'quiz'`, `Level1.tsx:9`, `Level2.tsx:12`, `Level3.tsx:14`) — the one structural thing it gets right. But L1 and L2 are the same exercise: `Level1.tsx:132` and `Level2.tsx:73` are character-identical checks with identical cheat sheets (`Level1.tsx:516-532` ≡ `Level2.tsx:428-445`). **L1 leaks on 12 of 12 items** — `Level1.tsx:64-73` prints `{Z}p+ {A−Z}n` unconditionally inside `BohrModel`, mounted in the quiz at `:352-356` gating only `showElectrons`. L2 has a one-click crib on 6 of 10 (`Level2.tsx:169-182` + a back button at `:216-221` that does not reset `quizIndex`). L3 leaks 2 of 8: the carbon hint at `isotopes.ts:273` states the key verbatim, and the chlorine worked example uses the exact numbers of quiz item `chlorine-avg`.

**Chemistry.** Clean. All 8 weighted averages recompute within tolerance, all abundances sum to exactly 100.00%, all 12 electron-shell configs correct. Two internal inconsistencies to fix on import: Li displays 6.95 (`Level3.tsx:337-338`) against key 6.94, and the flagship Cl example prints `26.50 + 8.96 = 35.45` (`:149-151`) where those rounded terms sum to 35.46.

**Icelandic — the long pole.** The rendered UI is diacritic-stripped ASCII across 43+ lines in the three level components ("Byggdu atomid", "Medalatoemmmassi"), with non-diacritic typos underneath (`medalatoemmmassi`, `samaeetur`, `Gefinnt`). The `is` i18n block at `i18n.ts:9-45` is the same; `:46-114` is correct Icelandic and is never rendered, because no level calls `t()`. Terminology corrected against the school corpus: `Prótín` (`isotopes.ts:50`) is _protein_ — protium is `prótíum`; `Dýterín` (`:61`) → `tvívetni`; `frumeindir` used for _subatomic particles_ → `öreind`. Credit where due: it uses `sætistala` for Z, which the corpus backs and shipped `lotukerfid` (`raðtala`/`atómnúmer`) does not.

**Fit.** A rebuilt Level 3 _inside_ `lotukerfid`, not a new game — `lotukerfid` already owns "Bygging atómsins" as its Level-3 heading, and an eighth Y1 node would cost the chain string in all seven Y1 `App.tsx` files plus `YearHub.tsx` and `build-games.mjs`. Salvage: `isotopes.ts:24-135` (10 isotopes), `:161-276` (8 verified weighted-average problems), `elements.ts:26-135`, the misconception copy at `i18n.ts:64-71`, `BohrModel` (`Level1.tsx:18-99`) and `IsotopeNotation` (`Level2.tsx:17-40`). Discard L1 and L2 as redundant with `lotukerfid` L3. **Note:** fixing the shipped bug changes live answer keys for Cu/Ni/Zn/Ga/Se/Br — students who already played will get different answers.

#### 2. `jonir-i-lausn` → **rebuild from content** (L)

**The deciding fact: the gap claim survives testing — all four topics are absent platform-wide — but Level 1 prints the answer above the buttons on 15 of 15 items, and the fix is a data migration, not a JSX edit.** `Level1.tsx:248` renders `currentQuizItem.description` above the answer buttons at `:256-288`; all 15 descriptions name the classifying property, 8 by literal stem-match ("Veik", "Sterk"), the other 7 by one mapping the teach phase states verbatim. A secondary cue compounds it: `Level1.tsx:244` renders the _unlit bulb_ pre-answer, which is the exact visual for "Órafkleyfi". Deleting `:248` leaves "NaCl. Which type?" — a bare recall drill. The `description` field has to be **split** across all 15 records in `substances.ts:61-240`: an observable non-classifying string before the answer, the rationale after.

**Coverage — the strongest absence finding in the set.** 34 greps in Icelandic and English over all 20 shipped games: `rafkleyfi` 0 · `raflausn` 0 · `rafleiðni` 0 · `jónast` 0 · `electrolyte` 0 · `conductivity` 0 · `botnfall` 0 · `útfelling` 0 · `net ionic` 0 · `áhorfendajón` 0 · `leysanleg` 0 · `Ksp` 0. The near-miss is real and worth naming: `1-ar/lausnir` _does_ teach "solubility" — but `TemperatureSolubility.tsx:21-66` is g/100 g H₂O against temperature (Brown §13.3, _how much_ dissolves), never _which_ ionic compounds dissolve. The two share an Icelandic word and nothing else.

**The content is the asset, and it is verified.** `reactions.ts:22-209` — 12 reactions, molecular _and_ net ionic equations, Icelandic explanations. All 8 precipitate equations balance including `2AgNO₃ + K₂CrO₄ → Ag₂CrO₄ + 2KNO₃` and `FeCl₃ + 3NaOH → Fe(OH)₃ + 3NaCl`; all 8 net ionic equations balance in mass and charge; all 4 no-precipitate cases name both soluble products. **This is the only net-ionic-equation content in either repo.** Plus 6 solubility rules verified against the school's own textbook table (`ch04/m68710-segments.is.md:35-118`) and 15 compound verdicts with no errors.

**Beyond the leak, what must be rewritten.** Level 3 tests without teaching (`Level3.tsx:12` has no `'learn'` phase) and tests an untaught rule (Ag₂CrO₄, with no chromate entry in `SOLUBILITY_RULES`). Its free-text grader is unusable: `normalizeFormula` (`Level3.tsx:37-43`) strips only whitespace and state labels despite a comment claiming otherwise, then does exact string equality against Unicode-subscript strings — **6 of 8 precipitates are unanswerable from an Icelandic keyboard**, and the placeholder instructs the student to type `BaSO₄(s)`. `Level3.tsx:81` sets `isCorrect = true` on any formula submission, inflating both `totalCorrect` and the accuracy readout. Base rate is 8 precipitate / 4 not — "always yes" scores 67%.

**Terminology, against the school's own OpenStax translation.** `Sterkt/Veikt rafkleyfi` (`substances.ts:30,42`) — the textbook is masculine, _Sterkur rafkleyfi_ (9 hits) / _veikur rafkleyfi_ (2); these render on every Level-1 answer button. `sterk/veik basi` (`:93,95,154,156`) → _sterkur/veikur_. `Saltpéturssýrulingur` (`:174`) → _saltpéturssýrlingur_. `Botnfallshvörf` → _útfellingarhvörf_ (10 hits; zero for the game's form). `jódíð` ×4 (`reactions.ts:47,51,55`, `solubility.ts:46`) → **joðíð** (53 textbook hits, 0 for `jódíð`) — and it lands on the very reaction the textbook uses as its worked example. Five sites render raw HTML entities as literal text (`Level3.tsx:294,295`, `Level2.tsx:190,310,318`) because entities inside JS string literals don't decode — **every one is on the wrong-answer path**, so a student who gets it wrong sees mojibake instead of feedback.

**Fit.** The textbook splits this: electrolytes are §11.2 (`ch11/m68781`), solubility and precipitation are §4.2 (`ch04/m68710`). The editorially clean shape is two deliverables — an electrolyte level inside `lausnir`, and a new _Útfellingarhvörf_ game between Jafna Jöfnur and Takmarkandi. (A single node after Lausnir is _also_ defensible, since §11.2 comes after §4.2; the split is a recommendation, not a constraint.) An Explore phase exists nowhere in the game — the obvious one is an ion-count slider driving `ParticleSimulation` next to the bulb, so strong/weak/non falls out as three regions of one axis.

#### 3. `hlutfallsgreining` → **rebuild from content** (L)

**The deciding fact: Level 3's scaffold _is_ the answer leak, so removing it deletes the level's only support.** `Level3.tsx:49` renders `({empiricalFormula}) × {multiplier} = {molecularFormula}` — the literal answer string — mounted at `:210` _below_ a still-live input at `:189-202`. Level 2's step table does the same one notch less blatantly (`Level2.tsx:110-114` prints the subscripts). **18 of 30 items expose the answer before commit.** Level 1's 12 expose it _after_ a wrong submit (`Level1.tsx:70`) — and because `disabled` requires _all_ answers correct (`:236-238`) and `pointsEarned = showHint || showSteps ? 5 : 10` (`:158`), submit-garbage-then-copy is the highest-scoring strategy at full credit.

**Coverage.** Empirical formula returns zero hits on 13 Icelandic and English terms across all 20 games; percent composition zero on 15. Both are named in `packages/shared/i18n/ordabok.md:193,328` and used by no game. The near-miss is exact: `molmassi/src/components/CalculationBreakdown.tsx:36-51` already renders each element's mass contribution directly above the compound's molar mass. **Both numerators and the denominator are on screen. Nothing divides them.**

**Chemistry: 27 of 30 sound, 3 defective, and the three break at different ends — do not patch them the same way.** `l2-5` (`compounds.ts:303-311`) is the only key error: the data gives N₂H₄O₃ (ammonium nitrate, recomputes to 35.00/5.05/59.96 exactly), the key says `NH₂O₃`. `l2-1` (`:262-270`) has _water's_ percentages presented as hydrogen peroxide — and the key `HO` is load-bearing, consumed by `l3-2` as H₂O₂'s empirical formula, so "fixing" the key breaks the L2→L3 thread; the fix is H 5.94 / O 94.06, verified to make the game's own `calculateSteps` derive `HO`. `l2-10` (`:354-362`) has percentages that reduce to no whole-number formula at all; they must become 27.74 / 23.56 / 48.69. Plus `l2-2`/`l2-8` are byte-identical duplicates labelled easy and hard.

**No teach phase exists anywhere.** `Level1.tsx:114-137`, `Level2.tsx:124-142`, `Level3.tsx:57-76` each mount straight onto problem 1. The "assume a 100 g sample" move — the conceptual pivot of the entire topic — is implicit in one division at `Level2.tsx:31` and stated nowhere. The grader is string equality (`compounds.ts:491-495`), so a student writing the properly reduced `Mg3P2O8` is marked wrong against `Mg₃(PO₄)₂`. Level 1 grades on a flat ±0.5 **absolute** percentage-point tolerance (`Level1.tsx:33,150`), so 2.5 passes for H in H₂SO₄ (true 2.06%, a 21% relative error); shipped `molmassi` scales tolerance by difficulty (`Level1.tsx:77-81`).

**Fit: two things.** Percent composition becomes a fourth `molmassi` level reusing `CalculationBreakdown` unchanged — a genuinely small job. Empirical + molecular formula becomes a new node between Mólmassi and Jafna Jöfnur. Salvage: 12 L1 compounds (all correct), 9 distinct L2 items (all usable after the three fixes), 8 L3 items (all correct), and the four-column algorithm table at `Level2.tsx:67-122`, **inverted** — from a reveal button into a live table the student types into, column-gated. That converts the game's worst defect into the platform's missing "live manipulable state" instance for this topic. Terminology: `sameindajafna`/`reynslujafna` for _formúla_ still renders at `i18n.ts:14`, `Level3.tsx:35,47`, `compounds.ts:299,319,340`, `index.html:7`, against `ordabok.md:193,357`.

#### 4. `markverdir-tolustafir` → **harvest content only** (M)

**The deciding fact: `dimensional-analysis` already grades significant figures and already demands scientific-notation answers, and the platform teaches neither.** DA's Level 3 renders a "Markverðir stafir: 3" tile in the given-information panel _before_ the student answers (`Level3.tsx:418-423`) and a ✓/✗ panel after (`:743-765`); `challenges.ts:265-273` asks for an answer in scientific notation with placeholder "t.d. 1.08e12". Nowhere in the repo is a rule for counting sig figs stated. Verified: `markverð` 4 hits (all DA UI labels), `marktæk` 0, `vísindarith` 0, `staðalform` 0, `aukastaf` 1 (an error string). `significant fig` and `scientific notation` return 21 hits — **all** in `utils/scoring.ts`, `utils/validation.ts` and their tests. Parser plumbing; zero student-facing text.

**Why harvest, not rebuild:** there is no shell worth keeping. The two-phase learn/quiz container is 40 lines of state that shipped `Level.tsx` patterns do better, and the interaction is a 10-item MCQ gauntlet with a 30-second countdown and a speed bonus (`Level3.tsx:18,33-43,93-95`) — shipped Y1 has zero timers. The content is `src/data/sigfigs.ts`, 532 lines, and it is the whole asset: 20 counting items, 12 calculation items, 12 scientific-notation items, and a 5-rule reference card (`:496-532`) whose trailing-zero convention _matches_ what `packages/shared/utils/scoring.ts:56-60` already implements — so harvesting introduces no convention conflict with the shipped grader.

**Two defects in the harvest.** The item selector discards 14 of 44 authored items (`Level1.tsx:20-24`, `Level2.tsx:20-24`, `Level3.tsx:22-26`), and it drops exactly the wrong ones: **every isolated captive-zero item** (sf5 `101`, sf6 `1.05`, sf7 `20.08`), so rule 2 survives only inside a compound hard item; and every bridge item (sf17–sf20, calc12) linking counting to notation. And `getAnswerOptions` (`Level1.tsx:31-45`) never shuffles — any answer ≥3 is always the third button, so clicking button three scores 5/10 without reading anything.

**Terminology needs a ruling before any string ships.** `vísindaritháttur` (16 sites) is a calque; the school's textbook uses **staðalform** (`appendices/m68860-segments.is.md:113`) with `veldisvísirform` as the synonym, and `vísindarith` returns 0 hits in the efni repo. `markverðir tölustafir` is a hybrid; the textbook says _markverðir stafir_ or _marktækir tölustafir_, and shipped DA already matches (`Level3.tsx:420`). `stuðull` for the mantissa collides with _conversion factor_, which is what `stuðull` already means in DA (`challenges.ts:277`) — the textbook calls it `tölustafaliðurinn`. And the **decimal separator** is load-bearing here, not cosmetic: the textbook writes `2,98 × 10⁵`, the game writes `.`, and `Level2.tsx:47` / `Level3.tsx:84-86` parse with `parseFloat`, so `13,8` becomes 13 and is marked wrong.

**Two shipped bugs found while checking coverage**, both in `1-ar/dimensional-analysis/src/data/challenges.ts` and both worth fixing regardless: `:271` keys L3-6 at `1.08e12` where `3.00e8 m/s → km/klst` is `1.08e9`, so a correct answer is marked wrong; and `:354`'s L3-12 is unsatisfiable — `0.5 mol × 58.5 g/mol` with 1 sig fig gives 30, which fails the 1% value check against 29.25, while 29.25 itself has 4 sig figs and fails its own `significantFigures: 3` check. No input passes both.

**Fit.** A "Stig 0 — Nákvæmni og staðalform" teaching level inside `dimensional-analysis`, or a two-tab expansion of `Level1Conceptual`, which is already declared the no-calculations screen. Not an eighth Y1 game: Brown treats sig figs as a subsection, and a new node costs nine files of chain edits. One component extraction is worth doing on its own: the superscript input palette (`Level3.tsx:288-302`, `× ⁰¹²³⁴⁵⁶⁷⁸⁹ ⁻`) into `packages/shared/components/`, because typing `10⁻⁵` on an Icelandic keyboard is a real problem DA already creates.

#### 5. `sydur-og-basar` → **rebuild from content** (M)

**The deciding fact: Level 1 prints the answer above the buttons on 15 of 15 items, and Icelandic makes the leak total.** `Level1.tsx:227-228` renders `name` and `description` above the sýra/basi buttons at `:253-285`. All 8 acids end `-sýra`, all 7 bases end `-hydróxíð`, and 9 of 15 descriptions state the category outright. The learn phase additionally pre-labels 8 of the 15 items with their category (`Level1.tsx:32-33,118-126,147-155`). Level 2 leaks 3 of 10 the same way (`Level2.tsx:79,120-135` renders the full balanced equation and salt name for the first three reactions, all of which are in the quiz).

**And Level 3's grader is unusable.** `Level3.tsx:14` sets `TOLERANCE = 0.5` and applies it as an _absolute_ difference at `:41`, regardless of unit — so for the two items whose answer is `0.10 M` (`problems.ts:158,242`), **any input in [−0.4, 0.6] scores, including 0**, and `:263-266` advertises `Svarsvið: ±0.5 M` to the student first. Both appear every playthrough. Shipped canon is relative: `lausnir/src/utils/validation.ts:43` and all 8 `ph-titration` Level-3 challenges use 2%.

**Coverage.** Arrhenius classification from a formula is absent — `nafnakerfid` has 59 compounds and **zero acids**; a Y1 student meets NaOH and Ca(OH)₂ as ionic-naming exercises and is never told they are bases. Neutralization as a reaction class is touched twice incidentally and never taught; `"sýra + basi"` and `"salt + vatn"` return zero repo-wide. Level 3's topic **is** taught, better, by `ph-titration` Level 3 (5 challenge types vs 2, including polyprotic and Henderson-Hasselbalch, with a correct relative tolerance) — though the repo's own `CURRICULUM_REVIEW.md:79` rates the Y1 half only ◐, so year placement is your call, not an automatic amputation.

**Chemistry is sound.** All 10 neutralization equations balance including the 2:1 and 1:2 cases; all 8 stoichiometry keys recompute correct with the mole ratio right in both directions; all 15 type assignments correct including the CH₃COOH trap. Two content flags: `substances.ts:175` calls HClO₄ "Sterkasta sýran", and `reactions.ts:202` offers Mg(OH)₂ — a reactant printed on screen — as a wrong-salt option.

**Icelandic is the worst in the set.** The game misspells its own subject: `i18n.ts` writes `sydur` nine times where the word is `sýrur`, and `sydur` is not even a valid ASCII stripping of it. 68 leaf strings in the `is` block contain zero diacritics, ~45–50 of which need them; 7 more are hardcoded ASCII inside gameplay (`Rett!` at three sites, `Grunnformula`, `Svarsvid`, `Algengt miskilningur`); and `substances.ts:28` sets `name: 'Syra'`, which renders on the L1 answer buttons. Outright errors: `Vetnisrómsýra` for HBr (`:124`), `Fosfórssýra` (`:148`), `Kolssýra` (`:208`), `Salpeturssýra` (`:100`), `hydróxíl` for hydroxide (`:40`), `stuil` for _stuðlar_ (`problems.ts:51`). And it contradicts shipped canon on nomenclature the student learned one node earlier: `hydróxíð`/`hýdróxíð` against `nafnakerfid`'s `hýdroxíð`, and `Natríumsalpetur`/`Magnesíumsalpetur`/`Kalíumsulfat` against `nítrat`/`súlfat` (`compounds.ts:276,303,321,348`). Applying "salpetur" — a trivial name for KNO₃ — to magnesium nitrate is simply wrong.

**Fit.** Two levels inside `lausnir` (already the solution-chemistry node, already 3,740 LOC) rather than an eighth game — 25 salvageable items is level-sized, not game-sized. Rewrite L1 as a sort-into-bins task where the suffix transparency becomes the _lesson_ rather than the answer key; rewrite L2 as a cation+anion assembler via `DragDropBuilder` so the student builds the wrong salt and sees it. The 30 hand-authored one-ion-swap distractors in `reactions.ts` are the best thing here and make per-error diagnosis derivable from the data.

#### 6. `flokkun-efna` → **rebuild from content** (M–L)

**The deciding fact: the central definition is wrong against the school's own textbook, and the answer is derivable from a rendered cue on every item.** `classifications.ts:58` defines heterogeneous purely by visibility ("Blöndur þar sem ólíka efnin eru greinilega sjáanleg"), and `:50` couples the correct "jafnt dreifð" with the same wrong criterion. OpenStax _Chemistry 2e_ `ch01/m68667.cnxml:62` is explicit: "A mixture with a composition that varies from point to point is called a heterogeneous mixture", repeated at `:237` and carried in the Icelandic at `02-for-mt/ch01/m68667-segments.is.md:173`. All 6 heterogeneous item hints reason from visibility (`:292,303,314,325,336,347`). None of the 24 keys is _mis_-keyed by it — the harm is that it installs a rule that fails the first time a student meets a fine powder. (The commonly-cited milk counterexample is **not** supported by this course's text, which places colloids between suspensions and solutions at `ch11/m68784-segments.is.md:14` and calls milk a _fleyti_; drop that illustration.)

**The leak.** All 12 pure substances carry a `formula`; **no** mixture does (`classifications.ts:72-206` vs `:216-350`), and it renders above the answer buttons in all three levels — so "is a formula printed?" separates pure substance from mixture with 24/24 accuracy and collapses the four-way choice to two, for free, in the level where speed is scored. In L1 and L2 the `description` also renders, and **10 of 12 mixture descriptions name their own category** ("Blanda af lofttegundum", "salt leyst upp í vatni", "Málmblöndun af kopar og tini"). Separately, all 8 L1 quiz items are the labelled examples from the learn screen, and the quiz's back button returns there without resetting `quizIndex`.

**Coverage.** Genuinely absent: `einsleit` 0 · `misleit` 0 · `samkynja` 0 · `ósamkynja` 0 · `sundurleit` 0 · `hrein efni` 0 · `efnablanda` 0 · `homogen` 0 · `pure substance` 0 · `mixture` 0 · `flokkun efn` 0. Element and compound are **touched as undefined prerequisites** — `frumefni` in 9 games, `efnasamband` in 2, every hit _using_ the words, none defining them; `nafnakerfid/src/components/Level1.tsx:304` asks "Hvernig þekkir þú hvort efnasamband er jónefni eða sameind?" of a student who was never told what a compound is. **Phases of matter are also absent** (`efnishamur`, `hamskipti`, `fasi` → 0) and belong to the same textbook section, so the hole is slightly larger than the game covers. Separation techniques do _not_ belong to §1.2 in this book — that recommendation, if made, is a design choice, not curriculum.

**Chrome.** This game carries every item the April 2026 restructure removed and one that no shipped game has: a 15-second countdown with auto-fail (`Level3.tsx:11,31-47,49-55`), streak bonuses, a `-50 stig` hint button (`Level2.tsx:37,123`), padlocks, and the full achievements surface with 5 notification containers. Both `maxScore` values are understated so a "perfect level" badge fires on an imperfect run.

**Fit.** A new game at chain position 0, ahead of Einingagreining — there is no host: `lotukerfid` is one taxonomy downstream, `nafnakerfid` two. But be honest that the topic is one textbook section: two phases, not three padded levels. Salvage `classifications.ts:66-350` (24 items with names, emoji, descriptions, hints; keys all correct), the four category records once `:50` and `:58` are rewritten, the classification tree layout (`Level1.tsx:91-165`), and the O₂ diatomic-misconception line (`:89`) into `FeedbackPanel`'s `misconception` slot — **not** `explanation`, which is collapsed by default at every call site. The right mechanic is a commit-before-you-see gate at the particle scale (one kind of ball / two bonded in fixed ratio / two free and evenly mixed / two in separate regions), which kills the visibility misconception structurally. Terminology: `Málmblöndun` → _málmblanda_ (`:255,257,279`), `Bökuð réttur` → _Bakaður_ (`:345`), `Lífsnauðsynlegt vökvi` → _Lífsnauðsynlegur_ (`:149`), and `index.html:7`/`package.json:5` say "sameindir" (molecules) where the category is _efnasamband_.

#### 7. `gerdir-efnahvarfa` → **harvest content only** (M for the level; the harvest itself is S)

**The deciding fact: 8 of its 20 equations are already in `jafna-jofnur`, and only 5 are genuinely new to the platform.** syn1=id1, syn2=id2, syn3=id3, dec3=id13, dec4=id11, comb1=id8, comb2=id15, comb4=id20 — plus sr2 (Fe + CuSO₄) is verbatim in `2-ar/redox-reactions/src/components/Level2.tsx:45-46`, and dec2 (H₂O₂) is in `3-ar/thermodynamics-predictor/src/data/problems.ts:27-28`. What remains is dr1–dr4 (the four double-replacement items, the only ones touching the acknowledged Chapter-4 hole) and dec1 (electrolysis of water).

**Reaction-type classification as a skill is genuinely absent** — `hvarfgerð`, `gerðir efnahvarfa`, `flokkun efnahvarfa`, `víxlhvarf`, `skiptihvarf`, `brunahvarf`, `botnfall`, `hlutleysing`, `nettójóna` all return zero. But the individual families are touched, and combustion more than the first pass found: `thermodynamics-predictor/src/data/problems.ts:8,78,98` and `hess-law/src/i18n.ts:86,102` name combustion under the `brennsla`/`bruna` stems.

**The leak is structural.** Level 1's quiz is the study material re-served: `Level1.tsx:21-22` slices the first two examples of each type into the learn phase and `:26-35` lists those exact ten ids as the quiz — **10 of 10 pre-answered**. Then the rendered `name`/`description` decides 7 of 20 items outright and narrows 4 more ("Bruni metans", "Vatn sundurgreinist", "Tvö sölt víxla jónum"); counting verbatim stem matches against the type descriptions the reference card shows, ~13 of 20. One item leaks the _wrong_ answer: syn3 is displayed as "Bruni magnesíums" (`reactions.ts:129`) but typed `'samsetting'` (`:124`).

**The terminology is blocked on you.** `Sundurliturarhvarf` (`reactions.ts:48`) is not an Icelandic word — _litur_ is colour — and the game contradicts itself, naming three of the four items in that very category with the glossary-correct root (`Niðurbrot`, `:176,191,206`). Worse, the shipped platform already has settled words for two of these five and the orphan contradicts both: **myndunarhvarf** for synthesis (`hess-law/src/i18n.ts:100`, `equilibrium-shifter/src/data/equilibria.ts:68`) and **sundrun**/**niðurbrot** for decomposition. Importing as-is puts a third and fourth vocabulary for the same two concepts in front of students. `Orðasafn í efnafræði.md` has `decomposition reaction;niðurbrotsefnahvarf` (`:132`) and `combustion;bruni` (`:101`) and **no entry at all** for synthesis, replacement or displacement — and the shipped `jafna-jofnur/src/i18n.ts:37` uses a fifth word, "Tvíbóta". That half needs a decision.

**One rendering defect worth knowing before any redesign:** `Level2.tsx:166` and `Level3.tsx:173` render `name.split(' ')[0]`, so in the two levels where the student is actually tested, two of the five answer buttons read **`Einfalt`** and **`Tvöfalt`** — bare adjectives with no chemistry noun. Whatever replaces `víxl-` has to survive first-word truncation.

**Fit.** A new level inside `jafna-jofnur`, carrying the 5 novel equations, the five type definitions and their abstract patterns (`reactions.ts:27-87`), and the real-world anchors (grillgas `:364`, bílvél `:379`, öndun-as-slow-combustion `:392-396`). The question should change from "which type is this?" (recognition against a visible menu) to "what forms?" (prediction) — which is what Brown 3.2/4.2 actually assess, and which turns the taxonomy into a tool.

#### 8. `stilltu-efnajofnur` → **harvest content only** (S)

**The deciding fact: superseded on its own topic by `jafna-jofnur`, which is better on every axis except pool size.** The shipped game has a teaching intro (`levelConfigs.tsx:3-86`), per-element Icelandic diagnostics (`balanceChecker.ts:101-113` names every unbalanced element with both counts), no score gate and no timer. The orphan has no teaching screen, a persistent header scoreboard, a `Vísbending (-5 stig)` label (`Level1.tsx:332`), a 3-minute countdown with a Skip button, achievements and padlocks. Strip all that and what remains is `jafna-jofnur`.

**What is worth taking.** All 35 equations balance (recomputed by hand, including `2Ca₃(PO₄)₂ + 6SiO₂ + 10C → 6CaSiO₃ + P₄ + 10CO` and `2KMnO₄ + 16HCl → 2KCl + 2MnCl₂ + 8H₂O + 5Cl₂`). Against the shipped 20: **19 overlap, 16 old-only, ~5 novel to the platform** — the only two precipitations anywhere in Y1 (AgCl, BaSO₄), the only three-product equation (Na₂CO₃ + 2HCl), water electrolysis, and the Claus reaction (S in two reactants and one product; `Claus` returns zero platform-wide). Plus P₄ + O₂ → P₄O₁₀, the only two-digit subscript in either game.

**Three constraints on the merge.** `jafna-jofnur/src/components/EquationEditor.tsx:79` hard-caps coefficients at 9 (`Math.min(9, value + 1)`, `aria-valuemax={9}`, and the value display is a `role="spinbutton"` div, not a typable input), which makes three items _unreachable_, not merely awkward — and the KMnO₄ one is a Brown ch. 20 half-reaction problem that arguably shouldn't be in a Y1 balance-by-inspection tier anyway. `balanceChecker.ts:80-95` hardcodes Icelandic names for exactly 14 elements and falls back to the bare symbol, so Ag, Cu and Ba must be added or the diagnostic silently degrades. And the 10 hard-tier hints are **mostly not usable as data**: `levelConfigs.tsx:109` sets `hintSource: 'dynamic-unbalanced'` for Level 3, so `reaction.hint` is never read there — the merge is viable for the easy/medium tiers only, and the shipped hard tier already has hints, just generated ones. Two other ideas are worth lifting on their own: the reaction-type badge shown _before_ you solve (`equations.ts:22-28,506-513`), and the **"Fela fjölda atóma"** toggle (`Level1.tsx:292-299`) — a scaffold fade `jafna-jofnur` lacks entirely.

One data-trust flag: `equations.ts:330` shipped `products: ['Cite', 'CO₂']` — literal garbage — patched by a module-level reassignment at `:494-503` rather than fixed in place. Re-verify every copied formula.

---

### Year 2 (three games)

#### 1. `calorimetry` (`Hitalitun`) → **rebuild from content** (L; a defensible case exists for M)

**The deciding fact, and the strongest evidence in this whole document: `apps/lab-reports/src/config/experiments/orka_2ar.ts` is a live, shipped, AI-graded second-year Kvennó lab called "Orka í efnahvörfum (∆H)" whose checklist grades students on exactly this material — and no game teaches it.** `:72-73` require `qlausn, qgler, qheild` for both salts; `:74` requires `mólarleysnivarmi`; `:86-87` require NaOH marked _útvermið_ and NH₄NO₃ marked _innvermið_; `:75` requires percent difference from the accepted value. The orphan's Level 2 items 1 and 5 are those exact experiments — NaOH in 100 g water warming 22→30 °C (`Level2.tsx:40`), NH₄NO₃ in 150 g water cooling 25→18 °C (`:88`). **The platform asks students to submit a calorimetry lab report and teaches calorimetry nowhere.** This is the only orphan topic corroborated by shipped production content rather than by a textbook chapter list.

**Coverage.** `eðlisvarm`, `varmarýmd`, `hitamæl`, `calorimet`, `specific heat`, `heat capacity`, `mcΔT`, `kaffibolla`, `sprengihitamæl`, `innri ork`, `ΔU`, `J/g`, `varmaflæði` — all zero across all 20 games. `ΔH` returns 311 hits and `kJ/mol` 85. So: **the interpretation of ΔH is taught twice over (hess-law, thermodynamics-predictor); the measurement of ΔH — every instrument, every formula, every unit — is absent.** `hess-law/src/App.tsx:339` already leans on it — "reikna ΔH með því að sameina jöfnur sem VER GETUM mælt" — while never telling the student how anything gets measured. Combustion ΔH _per mole_ is taught (`hess-law/src/data/challenges.ts:37`, `Level2.tsx:96-104`); what is absent is per-gram fuel energy content and any measurement of it.

**The leak is positional and total.** `grep -h "correctAnswer:" src/components/*.tsx | sort | uniq -c` → **`28 correctAnswer: 0,`**. All 28 items, every correct answer at array index 0, options rendered in source order with no shuffle anywhere in the game. Clicking the top-left option 28 times scores 280/280. Honest calibration: shipped `2-ar/rafeindabygging` has the identical defect at 8/8 in its Level 3, and six of twenty shipped games have no shuffle at all — so this is a real defect and a cheap fix (`packages/shared/utils/shuffle.ts:12` in a `useMemo`), not a discriminator.

**Three chemistry defects.** `Level3.tsx:128-133` computes 75.2 kJ/g for oil — arithmetically right, physically impossible, and it contradicts `Level3.tsx:85-89` _in the same level_, which teaches methane at ~55 kJ/g as the highest of four fuels. `Level1.tsx:113` derives c = 0.225 J/(g·°C) correctly and then says "svipað og ál (0.90) eða kopar (0.39)" — it is silver, 0.24, sitting in the game's own table two lines later at `:144`. And `Level3.tsx:64-69` and `:74-79` use opposite sign conventions five lines apart on identically-phrased questions, on a topic where sign is the main student error.

**Icelandic is the strongest argument for rebuild.** `hvarf` (n.) pluralises to `hvörf`, so `hvörfið`/`hvörfinu`/`hvörfsins` are ungrammatical — and there are ~30 such occurrences against 13 correct `hvarf`-stem forms. Add `brunihvörf` at 8 sites contradicting `Brunahvarf` at `Level3.tsx:54`, `syru` for _sýru_, `varmin` for _varminn_, `Óþekkt málmur` for _Óþekktur_, `gas-myndunnar`, and `strokkennu` (unparseable). Essentially every rendered question, hint and explanation is rewritten. The game's **name** is worse: `Hitalitun` appears zero times in `packages/shared/i18n/ordabok.md` and zero times anywhere in kvenno-app; the glossary's heat vocabulary is `varmi` (`:254`), `varmafræði` (`:551`), `varmaorka` (`:550`), nothing built on `-litun`. That needs a teacher ruling before anything ships. Also: `Exóþerm`/`Endóþerm` against `ordabok.md:216,195` (_útvermið_/_innvermið_) — though shipped `hess-law` uses raw English `exothermic` in Icelandic prose, so this one is platform-wide.

**What is salvageable and why L is the honest number.** 28 items with correct arithmetic (benzoic acid −26.46 kJ/g, sucrose −5,643 kJ/mol, NaOH −45.98, neutralisation −57 kJ/mol all match literature to 3 sf — someone looked them up), an 8-substance specific-heat table, 12 teaching cards, and the three-instrument curriculum spine (no calorimeter → coffee-cup constant P → bomb constant V), which is the actual conceptual content of Brown 5.5 and which no shipped game attempts. Against that: three level components that are byte-identical scaffolding (`diff` confirms `Level1.tsx:185-218` ≡ `Level2.tsx:191-224` ≡ `Level3.tsx:208-241`), zero interactive constructs in 1,388 lines, and **only 1 of the lab's 3 heat terms covered** — its sole calorimeter-constant item (`Level3.tsx:92-100`) is the _bomb_ constant, so `q_gler` and `q_heild` and the hand-warmer half of the lab must be written from scratch. A reviewer weighing only the string rewrite could reasonably land on M; the item that decides L is the content the lab needs and the game does not have.

**Fit.** A ninth Y2 game between IMF and Hess — measurement before manipulation. `FeedbackPanel` ports at zero cost (`diff` of the component and its types between the two repos: one blank line, and identical). Chain-string edits in all 8 existing Y2 `App.tsx` files.

#### 2. `electrochemistry` → **rebuild from content** (L)

**The deciding fact: two hard chemistry errors in 32 items, one of which teaches the reverse of the truth.** `Level1.tsx:94-99` marks _"Neikvæðar jónir (aníónir)"_ as migrating into the **cathode** compartment. Anions migrate toward the anode — which is what the word _anion_ means — and the item's own hint at `:98` argues for the correct answer, so it contradicts itself. `Level3.tsx:108-113` is unanswerable: 0.224 L H₂ at STP with I = 5.0 A gives t = 386 s, the key says 3859 s, and 386 is not among the four options.

**Coverage — a genuine quarter of Brown ch. 20 is missing.** `Faraday`, `96485`, `coulomb`, `Nernst`, `electrolysis`, `staðalmætti`, `afoxunarmætti`, `vetnisviðmið` — **zero hits, entire games tree**. `rafgreining` hits exactly one file, one prose word (`redox-reactions/src/App.tsx:300`). `virkniröð` — the platform's own glossary term at `ordabok.md:15` — hits only the glossary. Galvanic cell anatomy _is_ taught, and better: `redox-reactions/src/components/ElectrochemicalCell.tsx` is a live rAF particle animation with a cell selector and a voltmeter, against which the orphan's Level 1 is static divs whose only motion is `animate-pulse` on a fixed glyph. So roughly the first 10 of 32 items are redundant; the E° table (15 half-reactions, F₂ → Li⁺, values all standard) and the Faraday level are the gain.

**Leaks, two kinds.** Rendered: `GalvanicCellDiagram` prints `Anode (−)` over the metal symbol, `Cathode (+)`, an animated `e⁻ →`, and a legend reading `Anode: Oxun (Mg → Mg²⁺)` above every practice item — 6 of 10 L1 items decided literally, 2 more by trivial inference. Level 2 prints the decision rule `Hærra E° = Betri oxunarefni` (`:217`) above the four items that test exactly that rule, and two option sets carry their own E° values inline. Positional: **20 of 32 keys are at index 0** with no shuffle — clicking the first option scores 17/22 on L2+L3 (77%) and 20/32 overall against a 25% baseline.

**What is genuinely good, and thrown away.** `Level2.tsx:51` offers `-0.42 V` for Zn-Cu — exactly `0.34 − 0.76`, the classic sign error — and `:82` offers `-1.57 V` for Mg-Ag. Someone authored these to catch a named misconception. Then all three levels emit `` `Rangt. ${problem.explanation}` `` regardless of which distractor was picked. Wiring per-option diagnosis onto this existing distractor set is the highest-value work in the file. Chemistry otherwise checks out: all 15 E° values standard, all six E°cell items apply `E°cathode − E°anode` correctly including the reversed cell, five of six numeric Faraday items exact.

**Terminology is blocked and it touches shipped code.** English `Anode`/`Cathode` throughout the rendered UI against `ordabok.md:34,78` (`anóða`/`katóða`) — ironically correct in the dead `i18n.ts:37-38`. `sjálfvirkt` (_automatic_) for _spontaneous_ at ~9 sites against `ordabok.md:515` (`sjálfgengur`), with the game using the correct `sjálfgengi` at `App.tsx:190` — self-contradicting. `Galvanísk hlaup` (wrong gender, and _hlaup_ is jelly); shipped `redox-reactions` says `Galvanísk klefi`, also wrong-gendered, and `ordabok.md` has **no entry** for galvanic cell while consistently using a `-ker` root (`rafker` `:169`, `hálfker` `:251`). This needs your ruling and the fix lands in shipped code too. Also `hvörfið` at ~12 sites — `grep` for it across `apps/games` returns **zero**; the platform form is `hvarfið` in 6 files.

**One shipped bug found here.** `redox-reactions/src/components/ElectrochemicalCell.tsx:446` renders `{anode.metalSymbol} → {anode.ion} + e⁻` — unbalanced for **all four** selectable pairs, since every anode is 2+ — and `:454` likewise for 3 of 4. Both are inside `showLabels`, which defaults true, on the default `zn-cu` pair, so they are visible without any interaction. `:522` adds a hardcoded `+ 2e⁻` that renders `Ag⁺ + 2e⁻ → Ag` on the `zn-ag` pair.

**Fit: two homes.** L1–L2 become new levels inside `redox-reactions`, reusing the live simulation instead of the static diagram. Faraday's laws are mole-and-charge stoichiometry, not electron bookkeeping — a separate terminal Y2 node, with free numeric entry, because four options on a Faraday calculation reduce it to estimating a power of ten.

#### 3. `organic-reactions` → **harvest content only** (M)

**The deciding fact: organic reactivity is taught nowhere, but only ~14 of its 24 items are defensible.** Coverage is unambiguous — `viðbótarhvarf`, `brotthvarf`, `álagning`, `brottnám`, `skiptihvarf`, `kjarnsæk`, `rafsæk`, `bogaör`, `markovnikov`, `SN1|SN2|E1|E2` (the 7 hits are energy-bin locals in `kinetics/MaxwellBoltzmann.tsx`) all return zero. The shipped `organic-nomenclature` is naming only: no reaction arrow appears anywhere in it. A student can finish it, name a molecule, and not know what happens when you add bromine water.

**Level 2 should not be ported at all.** Its 8 items are definitional recall on nucleophile/electrophile/arrow-pushing/SN2/E2 — material with no Brown home (ch. 24 is descriptive organic and does not teach mechanism), pitched at 16–17-year-olds, and half of it leaks from its own on-screen reference panel: Q1←`:131`, Q4←`:154` (near-verbatim), Q7←`:143`, Q8←`:153`. One item (`Level2.tsx:41-50`) is ungradeable — `correctAnswer: 2` marks one option right while the reference panel rendered directly above it (`:141`) uses the wording of the option marked **wrong**.

**Level 1 leaks structurally.** `Level1.tsx:356-360` renders all three `ReactionTypeInfo` cards _during practice_, each stating its reaction's structural signature and a worked template, directly above the displayed `reactants → product`. Comparing the two answers all 10. Moving the panel to the learn phase — where the same components already appear at `:294-300` — fixes 6 of them; the other 4 have correct options that describe themselves against the visible equation ("Viðbótarhvarf - bróm bætist við tvöföld tengingu") and need re-authoring.

**Level 3 is the good part and needs almost nothing.** `ReagentBox` (`Level3.tsx:98-105`) renders a literal `?` and receives the answer only under `showFeedback && isCorrect` (`:317-320`): a real commit-before-you-see retrosynthesis gate, upphafsefni → **?** → markefni, and the platform's only instance of that shape in organic chemistry. Two caveats on import: the `steps` panel at `:392-401` is **not** gated on `isCorrect`, so a wrong answer displays the route containing the reagent, and `handleTryAgain` then re-offers the same four options for 5 points — the gate holds on the first attempt only. And drop item 6 (hydroboration–oxidation, AP scope, and the item carrying the untranslated "regióselectivity" and "(trialkylborane)").

**Drop item `Level1.tsx:69-78` too — it is wrong chemistry, not simplified.** C₆H₅Br + NaOH → C₆H₅OH + NaBr is presented as ordinary nucleophilic substitution with an explanation that generalises it to aromatic rings. Aryl halides are inert under any conditions a student would picture; the industrial route needs ~300 °C and ~200 atm and a different mechanism. Ported unedited it teaches a false generalisation that the surrounding items reinforce.

**Terminology.** Three of the four central terms contradict `ordabok.md`: "Viðbót" vs `álagning` (`:17`), "Staðgengill" vs `skiptihvarf` (`:536`), "Brotthvarf" vs `brottnám` (`:190`) — and _staðgengill_ in ordinary Icelandic is a deputy, _brotthvarf_ is dropping out of school. Mechanism, nucleophile and electrophile are correct. Honest note: **the glossary's reaction terms have never been applied in any shipped game** (`álagning`, `brottnám`, `skiptihvarf` → 0, 0, 0), so this is a decision to make, not a defect to fix. `Ýlingar` (`Level2.tsx:87`) is not a word.

**Fit.** ~14 items and 10 teaching slides become a new level inside `organic-nomenclature`, which is already the Y2 terminus — reaction types are the natural successor to naming. Not a new game: after the cuts this is level-sized. The one real build is per-distractor diagnosis; the shipped `MISCONCEPTIONS` record at `organic-nomenclature/src/components/Level1.tsx:10-15` is keyed by _question type_, not by wrong answer, so this is new authoring, not reuse.

---

### Year 3 (two games, plus two archived pH variants)

#### 1. `solubility-equilibrium` → **rebuild from content** (L)

**The deciding fact: `Ksp` has zero real occurrences anywhere in the platform, and the game's Level 1 scores the student against a different compound than the one on screen.** `Level1.tsx:106-110` closes over render-time `currentIndex`; `:153` queues the increment and `:154` calls the stale callback, so the options regenerate for the item just answered. With the interleave at `:85-90`, PbCl₂ is asked with AgCl's options, CaF₂ with PbCl₂'s, Fe(OH)₃ with CaF₂'s, BaSO₄ with Fe(OH)₃'s — **4 of 5 Ksp-expression items**. And `generateKspOptions` builds its "missing exponent" distractor identically to the correct answer for 1:1 compounds, so AgCl and BaSO₄ render **2 options, not 4**. AgCl — index 0 — is the only item that receives its own options, and it is one of the two binary ones. **Not one of the five presents a correctly-matched multi-option question.**

**Coverage.** Repo-wide, `\bKsp\b|samjón|sameiginleg jón|leysnimargfeldi|mólleysni` returns exactly two lines, both in `ordabok.md` (`:102`, `:501`). The words are in the dictionary and in no product. Molar solubility, Ksp expressions, percent-free selective precipitation, numeric Q vs Ksp: all absent. The common-ion effect is **touched but never named** — `equilibrium-shifter/src/data/equilibria.ts:312-341` runs `AgCl(s) ⇌ Ag⁺ + Cl⁻` with `add-product Ag⁺` as a Le Chatelier stress, so predicting the left shift _is_ the effect, and `samjón` returns zero in that game. `QKComparison.tsx:31-45` is fully qualitative — bar widths hard-coded 35/65 — so no Q is ever computed anywhere.

**The other leaks.** `Level1.tsx:339` prints `s = {calculateMolarSolubility(compound)}` on every card in the task that asks the student to rank by solubility — I ran the game's own function and every printed ordering reproduces the stored key. `Level3.tsx:372` prints Ksp on both selective-precipitation cards where both compounds share stoichiometry and concentration, so "lowest Ksp first" is the whole answer, with the rule spelled out in the hint. And `Level3.tsx:92-98,244-249` pre-computes the post-mixing dilution — the step students actually get wrong — on all 6 items, leaving a binary button. Also `Level2.tsx:212-216` prints the dissolution equation with its coefficients above the input on **every** L2 problem, unguarded, which is the entire difficulty of items 3 and 4.

**Two graders are broken.** `Level2.tsx:48` does `.replace(/10/g, 'e')`, so the format the game itself advertises at `:279` (`1.33×10⁻⁵`) parses to 1.33 and is marked wrong; `0.0000104` — the plain-decimal form of item 2's correct answer — parses to 0; `1,33e-5` (Icelandic decimal comma) parses to 1. And `Level3.tsx:92-98` halves concentrations the question text (`problems.ts:544,579`) states are already in one solution — the UI contradicts its own prompt.

**The content is the asset and it is sound.** All 21 answer keys recompute correct — L2 molar solubilities, Ksp-from-s, common-ion; all four L3 Q values and verdicts; both selective-precipitation thresholds; all three ranking keys; molar masses within ±0.03 g/mol across 20 compounds. `compounds.ts:327-333` implements the _general_ inversion `s = (Ksp/(xˣyʸ))^(1/(x+y))`, not the AB-only special case. `problems.ts:504-536` is the standout item: Q = 2.5×10⁻¹³ against Ksp = 5.35×10⁻¹³, same order of magnitude, the only "no" in the set — it defeats "mixing always precipitates" pattern-matching. One inconsistency to resolve before reuse: PbI₂ carries Ksp 9.8×10⁻⁹ in the table (`compounds.ts:69`) and the student derives 8.8×10⁻⁹ at `problems.ts:285`.

**Terminology and fit.** `sameiginlegu jónu áhrif` → `samjónahrif` (`ordabok.md:102`), `mólleysni` → `mólarleysni` (`:351`), `leysnimargfeldi` (`:501`) never used at all, `sértækt botnfall` → `hlutfelling` (`:229`), and `stefnumetri` (`problems.ts:30,93`) is not an Icelandic word — "hækkaðar til stefnumetris stuðla" is a literal transposition of "raised to their stoichiometric coefficients". A sixth Y3 node after Púfferar: Brown puts Ksp at 17.4–17.7, _after_ buffers (17.2) and titration (17.3), and `buffer-recipe-creator/src/App.tsx:305` already labels itself "Kafli 17". Note the pairing with `jonir-i-lausn`: that game is the qualitative question ("does a precipitate form, by rule?", Brown 4.2) and this is the quantitative one ("by Q vs Ksp?", 17.6), and they share anchor compounds (AgCl, PbI₂, BaSO₄) — shipping both closes one arc.

#### 2. `ka-kb-jafnvaegi` → **rebuild from content** (L)

**The deciding fact: the game's own 5% approximation rule is wrong by a factor of 20, and it stamps `✓` on four items where the approximation fails.** `level2-problems.ts:37-38` states the criterion as `Ka/C₀ < 0.05` and certifies it at `:54`, `:62`, `:83`, `:91`. Since x ≈ √(Ka·C₀), the real test x/C₀ ≤ 0.05 is equivalent to **Ka/C₀ ≤ 0.0025**. Recomputed: L2 Q2 6.00%, L2 Q3 5.83%, L2 Q5 6.63%, L3 Q3 6.00% — all four pass the game's rule and violate the real one, and L3 Q3 states 6.0% ionization where the exact quadratic gives 5.82%. A student following this carries a broken habit into pH Títrun.

**Coverage.** Fifteen Icelandic inflections of _ionization_ and ten acid/base-constant terms return zero across `apps/games`. `\bKa\b`/`\bKb\b` hits are TypeScript identifiers and data fields only — never rendered text (`ph-calculations.ts:67` computes `const Kb = KW / Ka` and shows nothing). `pKa` returns **425 hits**, all in `buffer-recipe-creator` and `ph-titration`, all of the form "compare target pH to pKa" — the conversion pKa = −log Ka exists only inside a utility function and a test. **The student is never shown where pKa comes from.** ICE tables: zero, in either language. Weak-acid dissociation exists in `equilibrium-shifter/src/data/equilibria.ts:248-275` as a qualitative Le Chatelier scenario with no K value.

**Three grading holes.** `Level3.tsx:28-32` accepts on substring match in either direction, so **a single letter scores full marks** on all three string items (`'f'` → 'formic', `'i'` → 'increases'). `Level2.tsx:26-36` routes L2 Q8 down the absolute-tolerance branch with `tolerance: 0.1` against a 0.050 M answer, so typing **0** is correct — and `:204-206` advertises `(skekkjumörk: ±0.1)` to the student. And 5 of 26 items leak: `Level2.tsx:157-162` prints `problem.acidBase.Ka` in the given-data panel on the two items that ask the student to _calculate_ Ka (Q7's panel value grades correct; Q6's grades wrong, so it is a trap rather than a leak), and `types.ts:31,61` allow only one `acidBase` per problem, so all four comparison items hold the answer and the UI points at it — L1 by printing the name, L3 by printing the K value the stem attaches to the answer.

**Three of eight Level-3 items require the student to type English.** `Level3.tsx:101,103` placeholder `increases / decreases / stays same` and `t.d. formic eða acetic`, against keys `'increases'`, `'formic'`, `'methylamine'`. And `Level1.tsx:178-210` renders `problem.options` raw with no `optionsIs` field, so items 9 and 10 show English prose options ("Formic acid (higher Ka)") under an Icelandic question.

**What is worth keeping.** `acids-bases.ts` — 18 substances with Icelandic names, Ka _or_ Kb, matching pK (17 of 18 exact to 2 dp; formic is 0.005 off), conjugate formula and name, and a real-world `context` string. Six duplicate `ph-titration`'s constants byte-for-byte, so **12 are new**; those 12 have not been checked against a literature table and need a teacher pass before reuse. The 8 ICE-table worked solutions in Icelandic and English. The persistent ICE-table reference panel (`Level2.tsx:279-325`) — method support that is not gated behind a hint and not penalised, which is exactly what the current standard asks for and which no shipped Y3 game has for weak-acid equilibria. And the dilution contrast pair: L3 Q1 and Q2 are the _same acid_ at 0.10 M and 0.010 M, with Q6 then asking what dilution does — the seed of a real Explore phase on the most counter-intuitive result in Brown ch. 16.

**Fit.** A sixth Y3 node between Jafnvægi and pH Títrun — the step where the general equilibrium constant becomes a specific acid constant and pKa acquires a meaning rather than a value. Chain-string edits in five verified locations plus `YearHub.tsx` and `build-games.mjs`. Terminology: it uses the correct `samoka basi` (matching `buffer-recipe-creator/src/App.tsx:128-129`) where shipped `ph-titration` says `samþjöppuð basi` — "compressed base" — in seven places. The orphan is right and a shipped game is wrong.

#### 3–4. `ph-titration-master` and `ph-titration-practice` → **skip** (S)

**These are not orphans, and the deciding fact closes an open question in this document.** Commit `54eafde` (2025-12-29, "Consolidate pH titration games into unified three-level game") moved **every** `ph-titration-master` file into `games/3-ar/ph-titration` as a 100% rename and archived both originals. That directory is what ships: `diff -q` reports `titrations.ts`, `indicators.ts` and `types.ts` byte-identical to `apps/games/3-ar/ph-titration/src/data/` and `src/`, and `ph-calculations.ts` differs by two TypeScript-narrowing hunks. Not one line of `-practice` crossed over. **So the archive pair needs no "second filter" explanation** — they were deliberately retired seven weeks before consolidation, and any glob over `games/{1,2,3}-ar/*` naturally misses `games/archive/`. Skip both; there is nothing here that is not already in the repo, in a newer version.

**Their value is entirely diagnostic, and it is the largest single finding in this document.** Reading them surfaced these live defects in `apps/games/3-ar/ph-titration`:

- **`src/utils/ph-calculations.ts:57` divides molarity by 1000** — `Math.sqrt(Ka * (molarityAcid / 1000))` gives pH **4.37** for 0.100 M acetic acid where the correct form gives **2.87**, and the game's own data asserts 2.87 (`titrations.ts:76`). Same at `:95` (weak base), `:133` and `:198` (polyprotic). `Level2.tsx:48` evaluates this on every render, so every weak-acid curve, flask colour and pH meter starts ~1.5 units too high. The existing test asserts only `1 < pH < 7` and passes. **`ph-titration-practice/src/utils/ph-calculations.ts:64` has the correct form — that one line is the most valuable thing in either archived directory.**
- **`src/utils/ph-calculations.ts:166` and `:223`** compute the final-equivalence conjugate-base concentration as `molesBase / totalVolume`, but at the second equivalence point `molesBase = 2 × molesAcid` — a factor-2 (and factor-3 at `:223`) error. The monoprotic analogue at `:66` is correct only by coincidence.
- **`src/data/level2-puzzles.ts` mismaps its titrations.** Puzzle 2 loads `titrationId: 4` (HClO₄ + LiOH, 15.0 mL 0.125 M, equivalence 18.75 mL at pH 7.00) under acetic-acid task text, accepts only phenolphthalein/thymol-blue, and explains acetate hydrolysis that never happens — **so a student who reasons correctly and picks bromothymol blue is marked wrong on both halves.** Puzzle 5 says HF and loads acetic; puzzle 6 says HCOOH and loads HF; puzzle 3 names the right substances with the wrong quantities. `Level2.tsx:236-241` prints the real values directly beneath the wrong task text and `:149-151` grades against the loaded titration. The `4→5, 5→6, 6→7` renumber fixes puzzle 2 only; the rest need prose or data reconciled.
- **`src/data/titrations.ts`**: `bestIndicator: 'phenolphthalein'` on a pH-7.00 titration (`:61`), which its own `isIndicatorAppropriate` rejects; `Saltssýra` at `:14,42,135,150`; four monoprotic weak-acid `equivalencePH` rows disagreeing with the curve by 0.07–0.16; oxalic `pKa2: 4.3` against Ka2 → 4.19; H₂SO₃'s stored `[4.5, 9.0]` about a full unit below what the drawn curve computes.
- **`Level1.tsx:255-257` renders a letter that contradicts its own text.** `level1-challenges.ts:38,44,50` embed `Kúrfa A:`/`B:`/`C:` inside the option labels, but `Level1.tsx:57-62` shuffles and then reassigns display ids `a,b,c,d` — so challenge 1 renders lines like `c. Kúrfa A: pH byrjar lágt…`, and the tier-4 hint names a letter the student may see as something else.
- **Hint penalties are live**, contradicting CLAUDE.md's "Phase 2: All hint penalties removed (code + UI text)": `Level1.tsx:87` scores `Math.round(100 * hintMultiplier)` and `Level2.tsx:454` prints `(50 stig)`.
- **`PHMeter.tsx` is orphaned** — it exists in the shipped repo and is imported by nothing. That was almost certainly deliberate (the archived master wires it into the play screen, which turns the task into scrub-until-the-number-jumps, and shipped replaced it with a marking commit gate) — but it should be deleted or wired on purpose.

**Two ideas from `-practice` are worth stealing** even though the game is skipped: the setup screen where the student enters analyte volume, molarity, titrant molarity and picks a Ka from a table of 5 weak acids and 4 weak bases (`App.tsx:152-321`) — no shipped Y3 game lets a student set titration parameters — and CSV export of the (volume, pH) series with an `is-IS` date stamp (`App.tsx:455-472`), which eight separate greps confirm is **absent platform-wide** and which is a real lab skill. Both would land as a "Þín eigin títrun" phase inside the existing Level 2. Note the archived sandbox leaks its own answers by default (`showHelperLines` defaults `true` at `:341`, drawing a `Jafngildi` line at the exact equivalence volume on all four configurations) — but the toggle to suppress it is already built at `:822-831`, so inverting the default and gating it behind a commit is the whole fix.

---

### What the fifteen share

**The template theory stays dead, and the generation-prompt theory is now confirmed by shape rather than by inference.** Fourteen of the fifteen ship a `README.md` that is the verbatim, unmodified game-template README — still titled "Kvennaskólinn Chemistry Game Template", still instructing the reader to `cd /home/user/ChemistryGames/tools`, still listing `GAME_NAME`/`GAME_ID` as placeholders to replace, still documenting `src/hooks/` and `public/` directories that in most cases do not exist. Meanwhile **every code placeholder was correctly substituted** in every game: real package names, real `vite.config.ts` output paths, real Icelandic `<title>`s. Several also ship a 0-byte `src/data/index.ts`. The generator wrote the game and never touched the docs.

**Fourteen of fifteen leak the answer. Only `stilltu-efnajofnur` is clean** (and even it has one hint that states "this equation is already balanced"). But the set reveals that "the answer leak" is at least four distinct mechanisms, and only one of them is what the shipped-games review found:

1. **Rendered prose** — the item's `name`, `description` or `hint` names the answer. `jonir-i-lausn` 15/15, `sydur-og-basar` 15/15, `flokkun-efna` ~10 of 12 mixtures, `gerdir-efnahvarfa` 7 outright and ~13 by stem-match, `organic-reactions` via its own reference panel.
2. **The scaffold _is_ the answer** — `hlutfallsgreining` Level 3 prints `(empirical) × n = molecular` below a live input, `Level 2` prints the subscripts, `solubility-equilibrium` prints molar solubility on the cards in a rank-by-solubility task. These cannot be patched by deletion; deleting the leak deletes the level's only support.
3. **Positional** — and this is the one no single game reveals. `calorimetry` **28/28 at index 0**, `electrochemistry` **20/32**, `markverdir-tolustafir` any answer ≥3 always third. Six of the twenty _shipped_ games have no shuffle either, and `2-ar/rafeindabygging` Level 3 has 8/8 at index 0 in production today. This is a platform-wide defect class, cheaply fixed with `packages/shared/utils/shuffle.ts:12` in a `useMemo`, that the review cycle never looked for.
4. **Reveal-then-retry** — the wrong-answer feedback names the answer and the retry button re-offers the same options at reduced credit. `organic-reactions` (all three levels), `hlutfallsgreining` Level 1 (at _full_ credit, because neither hint flag was set). Shipped `organic-nomenclature/src/components/Level2.tsx:429` does the same thing, so this one is not orphan-specific either.

**The Icelandic problems are worse and more varied than "ASCII-stripped".** The stripping is real in `uppbygging-atomanna` and `sydur-og-basar` and confirmed not to be an orphan trait. But four other classes cost more to fix: **coined non-words in student-facing text** — `stefnumetri` (solubility-equilibrium), `Hitalitun` as a game's _title_ (calorimetry), `Ýlingar` (organic-reactions), `Sundurliturarhvarf` (gerdir-efnahvarfa), `sydur` for _sýrur_ ×9 baked into a directory name; **systematic grammatical error on a core noun** — `hvörfið` for `hvarfið` at ~30 sites in calorimetry and ~12 in electrochemistry (zero occurrences of that form anywhere in `apps/games`); **contradiction with shipped canon** — `hydróxíð`/`salpetur`/`sulfat` in `sydur-og-basar` against `nafnakerfid`, two new words for synthesis and decomposition in `gerdir-efnahvarfa` against `hess-law`, `sætistala` in `uppbygging-atomanna` against `lotukerfid`'s `raðtala` (where the orphan is the one the corpus backs); and **English rendered in, or required by, the Icelandic UI** — indicator descriptions, `ruleEn`, `nameEn`, raw `options` arrays, and in `ka-kb-jafnvaegi` three of eight Level-3 items that cannot be answered in Icelandic at all.

**The i18n facade is near-universal and it is not an orphan trait.** Twelve or more of the fifteen render a `LanguageSwitcher` over is/en/pl dictionaries while their level components call `t()` **zero times**, so switching language changes the menu and leaves all gameplay in Icelandic. Several ship Polish blocks that are themselves diacritic-stripped. Shipped `2-ar/organic-nomenclature` has the identical dead wiring (0 `t('` calls, switcher rendered). Adopting any of these games imports the open `useGameI18n` decision CLAUDE.md already tracks for Y3 — a dozen times over.

**The chrome is present in all fifteen, but the calibration in this document's own Year-1 section needs correcting: the shipped platform is not clean either.** Hint penalties still ship in `2-ar/hess-law/src/i18n.ts:76` (`'Sýna vísbendingu (-10 stig)'`, rendered), `3-ar/gas-law-challenge/src/i18n.ts:34`, `2-ar/intermolecular-forces` Levels 1–3, `2-ar/lewis-structures/src/components/Level3.tsx:363-367` and `3-ar/ph-titration/src/components/Level1.tsx:87`. Streak bonuses ship in `3-ar/thermodynamics-predictor/src/App.tsx:127` and `3-ar/equilibrium-shifter/src/App.tsx:324` — in _every_ mode. 90-second timers ship in two Y3 games. A padlock ships on `equilibrium-shifter`'s challenge mode. **CLAUDE.md's "Phase 2: All hint penalties removed (code + UI text)" is stale**, which is the fourth time a recorded diagnosis in that file has turned out not to hold under measurement. What is genuinely unprecedented in the orphans is narrower and should be stated narrowly: **completion-gated teaching levels** (no shipped game gates a _level_), **the achievements surface** (zero production importers under `apps/`, only `vi.mock` stubs in one test file), and **hint costs advertised on the affordance in Y1**.

**One thing the set reveals that no single game does: the orphans and the shipped games have the same defects, because they came from the same generator.** The answer leak, the positional keys, the reveal-then-retry, the dead `LanguageSwitcher`, the hint penalties, the unrendered `misconception` slots — every one of them has at least one shipped instance. The April 2026 restructure and the review cycle removed a subset of these from the games that were present; the fifteen show what the whole set looked like beforehand, and by contrast they show precisely which defects the restructure never targeted.

**Two shared quantities worth recording.** Zero of the fifteen have any test file; against that, five of five shipped Y3 games and seven of eight shipped Y2 games do — and a `data-integrity` test is exactly what would have caught PbI₂'s two Ksp values, the three `hlutfallsgreining` percentage errors and the `ph-titration` equivalence drift. And `prettier --check` fails on 93 of 128 files while `eslint` blocks on only two games under kvenno-app's config (`ph-titration-master` 5 errors, `sydur-og-basar` 4) — and all five of the master's are the exact `any`-narrowing hunks the shipped descendant already fixed.

---

### Teaching ideas worth stealing

Each of these is worth lifting into a shipped game regardless of what happens to the game it came from — including the ones I would skip.

| Idea                                                                                                                                          | Source                                                                             | Where it goes and why                                                                                                                                |
| --------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Correct weak-acid initial pH** — `Math.sqrt(Ka * molarityAcid)`                                                                             | `ph-titration-practice/src/utils/ph-calculations.ts:64`                            | `3-ar/ph-titration/src/utils/ph-calculations.ts:57` — a one-line fix for a bug that puts every weak-acid curve 1.5 pH units too high                 |
| **Mass-number ≠ periodic-table mass** misconception copy, in real Icelandic                                                                   | `uppbygging-atomanna/src/i18n.ts:64-71`, `Level3.tsx:157-163`                      | `1-ar/lotukerfid/src/components/Level3.tsx:255-264` — replaces the "round the atomic mass" method that is currently mis-grading students             |
| **Per-isotope contribution reveal** — `mass × fraction = contribution` per line, then the sum                                                 | `uppbygging-atomanna/src/components/Level3.tsx:328-341`                            | the same rebuilt `lotukerfid` level; nothing shipped shows a weighted average this way                                                               |
| **Superscript input palette** (`× ⁰¹²³⁴⁵⁶⁷⁸⁹ ⁻` buttons appending to the input)                                                               | `markverdir-tolustafir/src/components/Level3.tsx:288-302`                          | `packages/shared/components/` — `1-ar/dimensional-analysis` already demands scientific-notation answers on an Icelandic keyboard                     |
| **The +− vs ×÷ two-column contrast** with digit-level highlighting on the limiting operand                                                    | `markverdir-tolustafir/src/components/Level2.tsx:105-139`                          | wherever sig-fig arithmetic lands; that layout _is_ the conceptual crux                                                                              |
| **Four-column empirical-formula table** (Prósenta → Mól → Hlutfall → vísitala), inverted into a live table the student fills column by column | `hlutfallsgreining/src/components/Level2.tsx:67-122`                               | a new `Reynsluformúlur` level — turns the game's worst leak into the missing "live manipulable state" instance and makes per-step diagnosis possible |
| **Per-element input rows** instead of one lump answer                                                                                         | `hlutfallsgreining/src/components/Level1.tsx:29-77`                                | `1-ar/molmassi` — it is exactly the input `diagnoseMistake()` needs                                                                                  |
| **`ConductivityTester`** — 38 lines of hand-rolled SVG, bright/dim/unlit bulb over a beaker                                                   | `jonir-i-lausn/src/components/Level1.tsx:12-49`                                    | `1-ar/lausnir` — the canonical Brown §4.1 apparatus, and the platform has no conductivity visual of any kind                                         |
| **Predict → then enter the formula** commit gate                                                                                              | `jonir-i-lausn/src/components/Level3.tsx:45-71`                                    | any precipitation content; the student commits before the third beaker is drawn                                                                      |
| **Reference-while-working lookup** (collapsible solubility rules available _during_ the quiz)                                                 | `jonir-i-lausn/src/components/Level2.tsx:183-209`                                  | anywhere — exactly right for "hints are never penalized", once the `-50` at `:42` is deleted                                                         |
| **One-ion-swap distractors**, hand-authored per reaction (30 of them)                                                                         | `sydur-og-basar/src/data/reactions.ts:40`                                          | makes the misconception derivable from the choice — "þú tókst réttan anjón en rangan katjón"                                                         |
| **The classification tree** — Efni → {Hrein efni, Blöndur} → four leaves, click-to-expand definition plus three worked examples               | `flokkun-efna/src/components/Level1.tsx:91-165`                                    | a Y1 position-0 game; it is Brown Fig. 1.9 as an interactive object                                                                                  |
| **Iconic atom circles** (one DOM circle per atom, count = subscript × coefficient) + the colour map                                           | `stilltu-efnajofnur/src/data/equations.ts:2-20`, `src/components/Level1.tsx:26-75` | `1-ar/jafna-jofnur` Level 1, whose `AtomCounter.tsx:20-56` is a four-column numeric table — conservation of mass rendered as an object               |
| **"Fela fjölda atóma" scaffold-fade toggle**                                                                                                  | `stilltu-efnajofnur/src/components/Level1.tsx:292-299`                             | `jafna-jofnur` — a genuine support fade the shipped game lacks entirely                                                                              |
| **Reaction-type badge shown _before_ you solve** ("this is a brunahvarf; balance it")                                                         | `stilltu-efnajofnur/src/data/equations.ts:22-28,506-513`                           | `jafna-jofnur` — reframes balancing, and carries the only reaction taxonomy in the set worth keeping                                                 |
| **Blank `?` reagent box** — upphafsefni → **?** → markefni, revealed only on a correct commit                                                 | `organic-reactions/src/components/Level3.tsx:98-105,317-320`                       | a new `organic-nomenclature` level; the platform's only retrosynthesis gate                                                                          |
| **Persistent ICE-table method panel** (I/C/E rows, the `x²/C₀` simplification), always visible, never gated behind a hint                     | `ka-kb-jafnvaegi/src/components/Level2.tsx:279-325`                                | `3-ar/ph-titration` and `buffer-recipe-creator` — no shipped Y3 game has a method template for weak-acid equilibria                                  |
| **Same acid at two concentrations, then "what does dilution do?"**                                                                            | `ka-kb-jafnvaegi/src/data/level3-problems.ts:6-41,98-113`                          | the Explore phase of a Ka/Kb node — the most counter-intuitive result in Brown ch. 16, discovered rather than told                                   |
| **General Ksp inversion** `s = (Ksp/(xˣyʸ))^(1/(x+y))`, not the AB-only special case                                                          | `solubility-equilibrium/src/data/compounds.ts:327-333`                             | any Ksp work — nine lines, correct for all stoichiometries                                                                                           |
| **Precipitate colours** on 20 ionic compounds                                                                                                 | `solubility-equilibrium/src/data/compounds.ts:9-267`                               | no shipped game has this data                                                                                                                        |
| **A negative case at the same order of magnitude** (Q = 2.5×10⁻¹³ vs Ksp = 5.35×10⁻¹³)                                                        | `solubility-equilibrium/src/data/problems.ts:504-536`                              | defeats "mixing always precipitates" pattern-matching — the only "no" in its set                                                                     |
| **Student-set titration parameters** (analyte volume, molarity, titrant molarity, pick a Ka from 5 acids / 4 bases)                           | `ph-titration-practice/src/App.tsx:152-321`                                        | a "Þín eigin títrun" phase in `ph-titration` Level 2 — "choose the acid, then predict the curve" is a move the platform cannot make today            |
| **CSV export of the (volume, pH) series**, `is-IS` date-stamped                                                                               | `ph-titration-practice/src/App.tsx:455-472`                                        | same place — a real lab skill, and eight greps confirm nothing on the platform exports data                                                          |
| **Progressive volume granularity with a subtract button** (+5 / +1 / +0.5 / +0.1 / −0.1) and full keyboard titration                          | `ph-titration-master/src/App.tsx:457-486`, `:236-266`                              | `ph-titration` Level 2, which has no way to back off an overshoot — modelling a lab habit                                                            |
| **Polyprotic curve rendering** — two or three shaded pKa bands with labels, a star per equivalence point                                      | `ph-titration-master/src/components/TitrationCurve.tsx:104-152`                    | already present but dormant in shipped `TitrationCurve.tsx:93-160`; one `LEVEL2_PUZZLES` entry and one relaxed cast wakes it                         |
| **The accessibility layer** — skip link, high-contrast mode, text sizing, reduced motion (written, never wired, in several orphans)           | `sydur-og-basar/src/styles.css:12-93`                                              | `packages/shared/styles/` — no shipped game has any of it, and the CSS already exists                                                                |

---

### Recommended order of work

Cheapest real win first. **Items 1–4 involve no orphan code at all** — they are defects this exercise found in the shipped platform, and they are worth more per hour than any port.

1. **Fix `1-ar/lotukerfid`'s neutron method.** `src/components/Level3.tsx:31-33`, plus the hint at `:106`, the intro at `:262` and the explanation at `:75`. Six of the seven mismatched elements round to radioactive nuclides; ~31% of playthroughs mis-grade. **S.** Tell the teacher explicitly that this changes live answer keys for Cu, Ni, Zn, Ga, Se and Br.
2. **Fix `3-ar/ph-titration`.** Four sites of `/1000` in `src/utils/ph-calculations.ts:57,95,133,198` with a regression test pinning 0.100 M acetic acid to 2.87; the factor-2/3 error at `:166,223`; the `level2-puzzles.ts` mismapping (start with puzzle 2, which marks the correct indicator wrong); the `Kúrfa A/B/C` letter collision in `Level1.tsx:255-257`; `bestIndicator` on `titrations.ts:61`; `Saltssýra` ×4. Decide `PHMeter.tsx`'s fate. **S–M.**
3. **Fix `1-ar/dimensional-analysis`'s two broken keys** — `src/data/challenges.ts:271` (`1.08e12` → `1.08e9`) and `:354` (unsatisfiable). **S.**
4. **Shuffle the option arrays.** `2-ar/rafeindabygging` Level 3 first (8/8 at index 0), then audit the other five shipped games with no shuffle. `packages/shared/utils/shuffle.ts:12` in a `useMemo`, three lines each. While there: `2-ar/redox-reactions/src/components/ElectrochemicalCell.tsx:446,454,522` render unbalanced electron counts on the default view. **S.**

Then the content work, cheapest curriculum win first:

5. **`markverdir-tolustafir` → a sig-figs and standard-form level inside `dimensional-analysis`.** Harvest only: `src/data/sigfigs.ts` (44 items, chemistry clean, trailing-zero convention already matching `packages/shared/utils/scoring.ts:56-60`) plus two teaching screens and one shared component. This closes a loop the platform is stuck in — DA grades sig figs and demands scientific notation and teaches neither. **⚠️ TEACHING DECISION FIRST:** `staðalform` vs `vísindaritháttur`, `markverðir stafir` vs `tölustafir`, `tölustafaliður` vs `stuðull` — and the **decimal separator**, which is platform-wide, currently inconsistent in shipped code, and load-bearing for this topic specifically. **M.**
6. **`uppbygging-atomanna` → a rebuilt `lotukerfid` Level 3.** Same body of work as item 1: isotopes and weighted atomic mass are absent, and the content that fills the gap is the content that explains why the shipped method is wrong. Salvage the three data files and two SVG components; discard L1 and L2 as redundant. **⚠️ TEACHING DECISION:** `sætistala` (corpus-backed, orphan) vs `raðtala`/`atómnúmer` (shipped) for Z — merging without picking one gives a game with three names for the same quantity. **M** for the narrow scope; **L** if you want the nucleus-assembly Explore phase.
7. **`hlutfallsgreining` → a fourth `molmassi` level plus a new `Reynsluformúlur` node.** Fix the three data defects first, each at a different end (key for `l2-5`, percentages for `l2-1` and `l2-10`), and delete the duplicate. The percent-composition half is genuinely small — `CalculationBreakdown` already renders both numerators and the denominator. The empirical-formula half is a build: three teach phases from zero, a parsed-element-map grader, and the step table inverted into a live one. **L.**
8. **`calorimetry` → a ninth Y2 game between IMF and Hess.** The strongest gap in the set and the only one corroborated by a shipped lab. Rewrite every Icelandic string, re-author three defective items, shuffle, and — the part that decides the effort — write the coffee-cup calorimeter-constant and hand-warmer content that the lab grades and the game does not have. **⚠️ TEACHING DECISION:** the game's name. `Hitalitun` is a coined word with zero occurrences in the glossary or the codebase. **L.**
9. **`jonir-i-lausn` → an electrolyte level in `lausnir` plus a new `Útfellingarhvörf` node.** Four absent topics, the only net-ionic content in either repo, chemistry verified against the school's own textbook. The Level-1 data migration (splitting `description` into observable and rationale across 15 records) and an Explore phase are the work. **⚠️ TEACHING DECISION:** whether to split it across two chain positions or place the whole thing after Lausnir — both are textbook-defensible. **L.**
10. **`solubility-equilibrium` and `ka-kb-jafnvaegi` → the two missing Y3 quantitative nodes.** Do them together and in that reading order — `ka-kb` sits between Jafnvægi and pH Títrun, `solubility` after Púfferar. Both have sound content (21 and 26 items, all keys recomputed correct) wrapped in graders that must be rewritten regardless. **⚠️ TEACHING DECISION:** adopt `ordabok.md`'s terms (`leysnimargfeldi`, `samjónahrif`, `mólarleysni`, `hlutfelling`) — none of which any shipped game has ever used — and check the 12 new Ka/Kb constants against a literature table before they go in front of students. **L each.**
11. **`electrochemistry` → two levels in `redox-reactions` plus a Faraday node.** Fix both chemistry errors, move Faraday to free numeric entry, wire per-option diagnosis onto the sign-error distractors that already exist. **⚠️ TEACHING DECISION, and it blocks shipped code too:** `anóða`/`katóða` vs the English currently rendered; `sjálfgengur` vs `sjálfvirkur`; and the galvanic-cell noun, where the glossary has no entry, the orphan says `hlaup` and shipped `ElectrochemicalCell.tsx:309,542` says `klefi`. **L.**
12. **`sydur-og-basar` → two levels in `lausnir`.** Content is sound and verified; the ±0.5 absolute grader and the 15/15 leak must go first, and the nomenclature must be reconciled with `nafnakerfid` or Y1 ends up contradicting itself one node apart. **⚠️ TEACHING DECISION:** whether neutralization belongs at Y1 at all — it changes what `ph-titration` may assume in Y3. **M.**
13. **`organic-reactions` → one level in `organic-nomenclature`.** Harvest ~14 items and 10 slides; drop all of Level 2, the aryl-halide item and the hydroboration item; gate the `steps` panel on `isCorrect`. **⚠️ TEACHING DECISION:** whether reaction-type classification is in the Icelandic Y2 syllabus (it probably is) and whether SN2/E2 is (it almost certainly is not) — building L1's content is a bet on the first answer. **M.**
14. **`flokkun-efna` → a Y1 position-0 game.** Rewrite both mixture definitions against `ch01/m68667.cnxml:62,237` before anything else; the 24 items and the classification tree are reusable. Be honest that the topic is one textbook section: two phases, not three padded levels. **M–L.**
15. **`gerdir-efnahvarfa` and `stilltu-efnajofnur` → one merged harvest into `jafna-jofnur`.** Between them, ~10 novel equations, the reaction-type taxonomy, and a scaffold-fade toggle. **⚠️ TEACHING DECISION AND IT BLOCKS EVERYTHING ELSE HERE:** the five reaction-type names. The glossary covers only decomposition and combustion; the shipped platform already uses `myndunarhvarf` and `sundrun`/`niðurbrot`; and `jafna-jofnur/src/i18n.ts:37` uses a further word, `Tvíbóta`. Nothing should be written until the five words are picked, because they are referenced by every level, every hint and the summary — and whatever replaces `víxl-` must survive first-word truncation. **S** for the harvest once the words exist.
16. **Skip both pH variants.** Their entire yield is items 1–2.

**Honest total.** Two skips, one S harvest, three M harvests, three M rebuilds, six L rebuilds, plus roughly a day of shipped-bug fixes. If you did every item this is many months of work; if you did items 1–7 you would fix the platform's worst live defect, close the two cheapest Y1 curriculum holes, and stop `dimensional-analysis` grading a skill it never teaches — and that is probably the right stopping point until the teaching decisions in items 5, 6, 8, 11 and 15 are made. **Seven of the sixteen items are blocked on a decision from you rather than on code**, and four of those decisions (reaction-type names, the decimal separator, the i18n question, and Z's Icelandic name) will produce inconsistencies inside a single curriculum chain if they are made per-game rather than once.
